# Tests

Headless Playwright suites for `index.html`. They drive the real page through the
`window.__magnet` debug hook (see `CLAUDE.md`), so they exercise the shipped code —
there is no build step and nothing is mocked.

The game itself stays dependency-free: Playwright is a **dev-only** tool and is never
loaded by the page.

## Running

```bash
npm i -D playwright          # once (or: npx playwright install chromium)
node tests/run.mjs           # every suite
node tests/run.mjs deck      # only suites matching "deck"
node tests/deck.mjs          # a single suite
```

The runner is **parallel** — each suite drives its own headless browser and then spends
most of its life waiting on it, so a serial run leaves the machine idle. 98 suites take
about **254s** six-up against ~535s one at a time. Output is printed in list order however
the results arrive, so two runs diff cleanly.

```bash
MB_JOBS=1 node tests/run.mjs   # force serial, for reproducing a flake
MB_JOBS=10 node tests/run.mjs  # a bigger box; every worker is a Chromium, so this is memory too
```

⚠️ **`ball3d` and `replayfile` are run LAST, alone.** Both assert on how long something
takes ("a small ball costs less than a big one"), and under a full pool they measure CPU
contention instead — both went red six-up and green serially, which is a runner reporting
load as a bug. A new suite with a timing assertion belongs in that list in `run.mjs`.

⚠️ **If a bare run dies with "Looks like Playwright was just installed or updated… run npx
playwright install", nothing was installed.** Playwright pins a browser REVISION per
release, so an upgrade — or a prebuilt image whose browsers were fetched by a different
version — leaves it looking for a build that is not on disk, and behind a network policy
that blocks the CDN the suggested command cannot work either. `_browser.mjs` now falls back
to the newest chromium actually present under `PLAYWRIGHT_BROWSERS_PATH`; `CHROME_PATH`
still overrides it.

If you have a browser already on disk, point at it instead of downloading one:

## Three traps that bite every new suite

⚠️ **`applyBundle('classic')` does NOTHING.** There is no `classic` palette — the keys are
`warp, chalk, pnp, synth, ammari, specimen, vector, ufo, board, sleeve, shrimp, pool, gameman, neon,
flat, grass, mono, vsoccer, light`. Several suites call it believing it resets the theme, and
it silently leaves whatever was applied last in place. Use a real key (`grass`, `neon`).

⚠️ **`render()` is only idempotent once SCREEN SHAKE has decayed.** Shake jitters the whole
pitch by `Math.random()` on every draw, so with any left over, two shots of one state differ
everywhere. `decayJuice` is step-locked, so a block that never steps must wind it down by hand —
and note `spawnKickFx` ADDS shake, so a block that spawns sparks has to decay it *afterwards*
too. Leftover sparks are the same hazard: they only age in `advanceFx`, so anything alive is
drawn over every later pixel check. Both have broken blocks in suites that never mention fx.

⚠️ **`getImageData` outside the canvas returns zeros**, which reads as "that area is black" —
the most convincing wrong answer available. A world point "60 units past the touchline" lands
off the bitmap at some viewports; sample in screen space and clamp.


```bash
CHROME_PATH=/path/to/chrome node tests/run.mjs
```

Every suite exits non-zero on failure, so `run.mjs` fails loudly — and
`.github/workflows/tests.yml` runs it on every push and pull request.

CI installs Playwright with `npm install --no-save --no-package-lock playwright@<pinned>`,
which writes nothing but `node_modules`: the repo deliberately carries no
`package.json` and no lockfile, so the version is pinned in the workflow instead.
Bump it there when you want a newer Playwright (the browser cache key uses the same
version, so it refreshes with it).

## Suites

| Suite | Covers |
|---|---|
| `smoke` | Duplicate element IDs, every screen/picker/theme/drill/mode/party combo opens and runs without throwing |
| `ball_grass` | Ball rolls (incl. multi-ball extras), all six grass patterns render, picker builds |
| `kickoff` | Kickoff line holds both teams, releases on play, bots obey it, rule-off frees movement |
| `coop` | Controller routing: co-op fills your team first (N humans vs N AI), versus interleaves, overflow |
| `drillfeel2` | Drills use your Game Feel / pitch / magnet settings rather than fixed defaults |
| `deck` | Steam Deck layout: camera turn, pitch fits, focus ring, collapse frees the pitch, containment |
| `deck2` | Controller menu depth: card headers expand, section jumps, slider adjust |
| `deckctrl` | Stick maps to **screen** directions in all four axes with the pitch turned |
| `deckreg` | Deck renders every field/grass/flag with zero ball or player escapes; mobile unaffected |
| `deckpad` | Pad ownership: game owns the pad while collapsed, A never reopens the menu |
| `deckmenu` | Menu only takes the pad while actually open; Select always toggles; drills hand over too |
| `orient` | Pitch-direction setting drives camera **and** controls together; cocktail stays upright |
| `fs` | Full screen genuinely enters on click and exits on F, label tracks state |
| `labels` | Name plates fade to 5% over a disc or the ball and return when clear, both orientations |
| `unlocked` | Unlocked summary: counts match the unlock model, strip shows only earned items, tap equips |
| `demo` | Idle demo uses two random countries (not your look), and the Demo tag paints only in demo |
| `debug` | Debug readout gated by its toggle, numbers track the live sim and feel values, build stamp |
| `demo2` | Demo picks a random court and never replays; the demo is a **showcase** — top-tier bots on both benches (derived from the last `DIFF` key, not spelled), a real 3-minute clock, ignoring the player's own difficulty and length, and a **level demo ends rather than going to overtime** (it would hang on the menu until someone scored) while a real match at the same point still gets sudden death; controller wording; crowd cheers — the three Stadium-family ones longer than the three originals, all distinct, and **every** SFX category's variant count matching its label count (an unlabelled sound is unpickable). Counts are derived, not pinned: themed sets add variants, and a suite that says "exactly six" only measures how recently someone edited it |
| `killerlobsters` | Killer Lobsters: **the snail is kickable and still heavy** — one kick moves it twice as far as a full-speed body check while the same kick sends the ball twenty times further, it is slower off the boot than the ball, it can never be trapped, and holding KICK is one shove rather than a shove per frame (⚠️ measured ACROSS the pitch on a big court: kicking toward a goal ends the match or re-serves the ball, and the first version read that reset as travel and made the snail look lighter than the ball). Plus the heavy non-resetting snail, goals that don't reset play, snail = instant win, and exactly one *scoring* ball among the bodies — the body count is derived from `BERRY.count` rather than pinned at two, because the mode gained berries and a pinned count only measures how recently someone edited it (see `kqberry`) |
| `grasstiles` | Grass cut tiles draw the selected court with each mow pattern, and field/grass/theme picks redraw each other |
| `trapwindow` | Trap window slider changes how long the ball actually sticks, applies live, survives reset/presets/drills, and one-touch ignores it |
| `offline` | **Download the game and play it offline** — the single-file copy that matters most on Linux. Holds: the button is in About and is hit-tested rather than merely present; over http it saves a file named for the running VERSION and reports the size; the saved bytes are the SERVED SOURCE, byte for byte; and the copy BOOTS AND PLAYS a match from `file://` with no server and no assets folder beside it. ⚠️ The trap is `document.documentElement.outerHTML` — the obvious way to save a page, and wrong, because it is the page as it is right now with every menu class toggled and a match mid-run. ⚠️ An outerHTML copy still boots and plays, so "the saved file works" does not catch it: the suite dirties the DOM first (starts a match, appends a marker node) and requires byte-identity with what the server sent. Verified by sabotage — the outerHTML build failed on the byte count and on the leaked marker, having passed every play-the-file check. ⚠️ Missing artwork logs ERR_FILE_NOT_FOUND per file, which is the documented graceful fallback, so console noise is filtered to real script errors |
| `onehand` | One-handed mode: releasing the stick shoots (never traps), holding doesn't, jitter doesn't, cooldown caps the rate, KICK still works | ⚠️ Plus **sweeping across the centre is not a release**: the pulse armed and fired on magnitude alone, so dragging a thumb from one direction to the opposite one — never lifting — crossed the deadzone and fired a shot, which is to say every time you turned round. Driven through the REAL `onDown`/`onMove`/`onUp` on a **second, phone-sized page**: the fix reads `pad.move.id`, which only the touch handlers set, so writing to `pads.p1` tests the old path, and the suite's own 1280×800 page never returns a `move` zone at all. Guarded from both ends — the thumb is asserted to have actually reached the dead centre, and a genuine lift must still fire. |
| `arcade` | **The standup arcade cabinet** — four panels, one keyboard, no gamepad connected at all. Holds: the layout takes seats on its own (the Controllers setting is left at its default, deliberately) and presents four panels as four virtual pads, so 2v2 fields four people and 4v4 with Extra controllers on Co-op puts all four against four bots; the ordinary keyboard seat **stands down**, or P1's stick drives two players at once; a panel stick is digital, ±1 an axis; coin-op refuses with no credit while free play charges nothing but still counts the game; a held coin switch banks **one**; START starts a game from the result screen and is ignored mid-match; the takings persist and are kept **out of `sel`**; the operator screen builds, the service key (F2) opens it and the reset arms first; and a cabinet is **not** a touch layout. ⚠️ Every claim is checked with the layout switched back off, or they all pass on a build where every layout behaves that way | ⚠️ Three measurement traps. **The `e.code` assertion is about the collateral, not the press.** A numpad key's `e.key` is never its own name — NumLock on, `Numpad6` is "6" (P2's coin slot); NumLock off it is "ArrowRight" (P1's stick) — so an `e.key` build cross-wires two panels either way, but P4 still moves. "P4 goes right" passes on the broken build; "and nobody else moved" is what catches it, and Chromium drives synthetic keys NumLock-off so that is the half this measures. ⚠️ **`keyboard.down` does not auto-repeat**, so holding a key and waiting proves nothing about the `e.repeat` guard — it is one keydown either way and the guard can be deleted with the suite green. The repeats are dispatched by hand as `KeyboardEvent('keydown', {repeat:true})`, which is what an OS sends. ⚠️ **`toMenu()` is not "no game in progress"** on a desktop-sized window: the menu is a left dock and the match on the right keeps playing, so START is right to ignore it there and a probe built on it fails for the wrong reason. The over-the-match case is driven by ending the match; the attract demo is checked through `arcadeIdle` directly |
| `audit` | Every setting is **reachable** (a real control writes it) and **effective** (it moves world state or pixels); every nav tile, drill and mode runs |
| `surfaces` | Ice and mud are visibly distinct and textured (not a flat tint), mud starts green and both **wear in from actual play** (subtle, distance-gated, reset per match), deterministic, cache invalidates, tiles differ, grip unchanged, every field safe |
| `seatnames` | Player names typed in settings land on seats in order, blanks keep defaults, overflow/long names safe, demo stays generic |
| `mobilefit` | Phone framing: pitch clears the thumbsticks and leaves no dead slab under the HUD; desktop untouched |
| `resultfit` | The result screen **on a phone**, which was unusable: a 3v3 at 390×844 measured 1087px inside an 844px window, and `#overlay` was a `justify-content: center` flex column with `overflow: visible`. Those two together are fatal — centring an overflowing column puts the title at y = **−271** with no way to reach it, and nothing scrolls, so Restart / Warm-up / Main Menu at y = 845…1046 could not be pressed either. A touch-only player who finished a match was stuck until the 30s auto-advance. So: (1) **nothing sits above the viewport** — no child of `#overlay` has a negative top, folded or open; (2) **every action is pressable**, measured by scrolling to it and hit-testing its centre with `elementFromPoint`, not by asking whether it happens to be on screen — the screen scrolls, so below the fold is fine and *unreachable or covered* is not; (3) no player exceeds the ribbon cap and the surviving two are the top of the priority order; (4) the **scoreline is one side-by-side row at the top** — measured against the panels being *stacked* in the same render, because "they are next to each other" is trivially true on a desktop-shaped page, which is the layout that never had the problem; it is above both panels, the row cannot wrap, the winner's box is marked, and the score is not printed a second time (no panel score, no scoreline left in `#ovSub`); (5) the per-player table **folds on a phone** (hidden, never deleted — the score and ribbons stay because they are the result, not a breakdown of it), the heading is a 44px touch target, a tap restores every row and prose line, and the folded screen fits one screen while the open one genuinely overflows and scrolls to the bottom. Desktop starts open and keeps all six rows. Verified to fail on each of the three sabotages: `overflow: visible`, cap raised to 99, and the fold rule deleted. ⚠️ **Wait for the SLAM to finish before measuring**: the title runs `@keyframes slam` from `scale(2.6)`, and `getBoundingClientRect()` reports the *transformed* box — mid-animation a title correctly laid out at y=24 measures **y=−16** and check (1) fails on a screen with nothing wrong with it. The desktop case tripped over this for real; the phone case had been passing on timing luck |
| `cocktailnopad` | Cocktail with **no** controller connected stays playable — the keyboard falls back rather than leaving nothing driving the player |
| `livelook` | Customising your player shows on the pitch immediately (no restart); the idle demo has no clock, never announces OVERTIME, never throws a result overlay, and never wears your look |
| `keyfocus` | Space **and** X kick, neither steals a keystroke while typing, clicking the pitch releases a focused slider so arrows drive the player again, Game Feel splits into ball/player groups | ⚠️ The Game Feel grouping check moved from `.subhead` text to the **tabs**: the card was split into five panes and the chip row became the heading, so the subheads went. Same claim, checked where the grouping now lives — a chip you can press against a pane holding the right sliders.
| `padflairs` | A controller icon per connected pad, bottom-right; goes black on a lit chip while any button is held; nothing drawn with no pads; 4 pads draw more than 1 |
| `accordion` | Settings sections open one at a time: clicking one closes the rest, clicking the open one closes it, deep links obey it, a stale multi-open state is repaired on load |
| `panel` | `/settings` route: same cards as inline, no game, snapshot on open, two-way live sync, telemetry, detached/inline, cross-tab match control |
| `botlook` | Bots wear their own face/cap/eyes — never yours — vary from each other, and stay stable across restarts | Bots wear **no cap** (they used to cycle the whole CAPS table); they stay individuals through colour, shirt number and eyes.
| `cocktailkeys` | Cocktail takes the keyboard off the pitch and seats player 1 on a controller; picking it keeps you on the menu able to kick off; pitch direction is locked and says why |
| `tells` | Motion tells by **canvas pixel sampling** (incl. the charge ring flashing as a full circle rather than sweeping): moving players leave ink, parked ones don't, ball streak scales with speed, wind-up shows on the disc, and both read on **all six themes** |
| `floaters` | **Floating stat text** — GOAL / ASSIST / SAVE over the player who earned it. The load-bearing check is that **the label and the stat move together**: each case drives the real scoring path and asserts both `ms.<stat>++` and a label on *that* player (an assist must NOT also land on the scorer — a label that fires on everyone passes a "did ASSIST appear" check and is useless). Plus: a plain TOUCH produces nothing, a draw does not age a label while a step does and on time, the cap drops the **oldest**, the toggle defaults on and off spawns nothing, and no `Math.random` on the spawn path (it is reached from `step()`). ⚠️ Four measurement traps, all of which passed a sabotage before being fixed: don't time expiry in a LIVE match (bots keep spawning labels, so the count never reaches zero); **zero `shake` first** — it jitters the whole pitch by `Math.random()` every render, so two identical frames differed by ~19,000 pixels and the label diff was measuring the wobble; **discard a warm-up render** (the first `render()` after `startMatch` builds caches, worth another ~19,000); and for the clamp check, take the baseline with the body **already** at the edge and place it **genuinely off-screen** — diffing against a centre-pitch frame measures the disc moving, and `W/2 + 30` is still comfortably on screen | ⚠️ Plus the **2-second cooldown**: one of each label per player, reported as the same word printed over somebody two and three times (a ball rebounding off a wall is a second legitimate strike a few frames later, so the play was right and the caption was noise). Pinned from both ends — a burst gives one label, a *different* label on that player still fires (a blanket cooldown would swallow the GOAL! after the SHOT that scored it), the same label on another player still fires, it is still held just before the gap and comes BACK after it, and a reset clears the cooldown with the labels. **The stat is asserted untouched** through the real `noteKick`: four strikes counted, one caption. ⚠️ **A sabotage exposed this suite's own blind spot** — every timing check derives its tick counts FROM `FLOAT.cool`, so they are scale-invariant and a constant changed to 30 passed all of them; the two seconds that were actually asked for is now asserted as an absolute alongside them.
| `themetiles` | The two grids in the Theme card are different pictures: the **Background** slot shows each palette as a painted canvas (no emoji) — one tile per palette, all distinct, every band sampled back to that palette's own colours — while the **Bundle** row shows the whole collection (its field, its players, its ball) and is asserted to differ pixel-wise from the palette tile it sits above; picking a palette alone switches the colours *and* makes the theme Custom |
| `contrast` | No label sits on a colour too close to it: every visible text element on every screen under every theme is held to WCAG AA against its **composited** background, plus the goal banner's outline; includes a known-bad probe so a clean run can't be vacuous. ⚠️ **The theme colour TRANSITION is switched off, not waited out.** `body` carries `transition: background-color .2s, color .2s`, so a sample taken mid-fade reads one theme's ink over another's paper — caught as Ammari's `#f4f2f4` on Specimen's `#e5d501`, a pair that exists in no palette. A 320ms wait was the first fix and it is only a MARGIN: this loop rebuilds the whole settings screen 20 times, so anything added to `buildSettings` eats into it, and the suite failed about one run in four. Verified both ways — with the fade on and 6ms waits it invents **365** failures, with the fade off and the same waits, none |
| `awards` | End-of-match awards state the figure that won them ("Most Saves · 5 saves"), the number matches the tally that picked the winner, singulars stay singular, and it reaches the DOM. ⚠️ Plus the two rules that stop one dominant player becoming a wall of ribbons: **no player holds more than `AWARD_PER_PLAYER`** (every award is a `topBy`, so a real 3v3 handed the same name all eight), and **Hat Trick REPLACES Most Goals** rather than joining it — same stat, same `topBy`, same note, so shipping both printed one fact twice; below three goals Most Goals is unchanged. ⚠️ The tally here spreads its maxima across six *winners*, not just six values: the original piled goals, key passes, posts and the hardest shot onto player 0, which is exactly the case the cap removes, and the suite then read the cap working as four missing awards |
| `kickpush` | Optional "kick off walls & players": off by default and provably inert when off; on, a wall/arc/post launches you *away* from it on all four boundaries, charge scales it, a body takes the shove while you take the smaller recoil along the same line, empty space and out-of-range targets do nothing, one press is one launch with a cooldown, it never fires at kickoff or after the whistle, the ball is untouched, and the picker writes and persists |
| `lobby` | Warm-up lobby + the game-over chooser: pads in play drop into `warmup` first (keyboard-only doesn't), bots the plan needs **walk on** to the middle of their own half and settle there (surplus ones walk back off), re-balancing live when everyone crowds one side, and they never run the AI; the ball can't be disturbed, sticks and KICK are testable; standing on a half picks your team and standing **outside** sits the match out, sides always come out equal (2v1 → 2v2, solo auto-assigns), **everyone on one side stays together** — six controllers on one half is a 6v6 against bots, not 3v3 against each other — with bots built to order when the mode's roster runs short and the roster settling rather than growing a body per trip to the lobby; the mode's seat count is the real ceiling (a 9th pad in a 4v4 is never seated); a benched player still moves but can never re-enter mid-match and can't wander off screen; cocktail calibration takes one arrow at a time, ignores short holds and face buttons, accepts stick or dpad, persists the seat's side and is proved by the player then moving *up* the screen; the result screen offers Restart (default, same teams) and Warm-up, and only **Player 1's** pad can move the selection |
| `lobbyballs` | **Warm-up balls** — one per half, one more per person, and a `ballOnly` wall down halfway. Holds: the count follows the room (somebody joining mid-lobby brings a ball, leaving takes one, a ball being dribbled is never teleported home by the resync); the wall genuinely contains a ball, **driven** at 14 into the line and required to stay on its half and come back off it; a **player walks straight through** it, because walking into a half is the whole mechanism for choosing a side; and nothing leaks into the match, from four directions — Start, the idle auto-start, a fresh `startMatch`, and ⚠️ **`resetKickoff` on its own**. That last one is the point: every exit that exists today runs through `lobbyStart` or builds a new world, so moving the cleanup into `lobbyStart` passes the other three — measured, not assumed. ⚠️ Also traps: never check containment by looking for a `lobbyWall` entry in `w.walls` (that measures a push onto an array — the wall could be zero-length, on the wrong axis or not `ballOnly` and still pass), and sample the bounce over the WHOLE run rather than the last frame, since `damp` leaves `vy` a coin toss by frame 60 |
| `lobbykb` | **The lobby keyboard**: walk onto letters to spell your own name, then walk into the pitch. Three things have to be true at once and each alone is satisfiable by a build that does not work — the letters are REACHABLE (`integrate` clamps every body to the pitch plus a margin, so this is driven with the stick for real, never by teleporting a body onto a key, which passes on a build where the clamp was never widened — the `targetsdrill` trap), VISIBLE (every key's screen box inside the canvas), and not UNDER anything (the fixed `#lobbyStartBtn` and the lobby headline are measured as real rect overlaps; the first build drew the bottom two rows of keys beneath the Start button). Plus: every key outside the back of the net and standing on one is not a side pick, a dwell types while walking past is silent, the press is latched (three seconds on one key is one letter), the first press clears the old name, DEL/SPACE, the 12-character cap, bots never type, the name commits to the PROFILE at the whistle, the keyboard is gone in play, the pitch really is smaller with it than without, and nothing advances in a draw. Also run on a phone, where the top and bottom reservations are tightest. ⚠️ **Three traps recorded.** A pad must be connected or there is no lobby at all (`warmupUseful()` is false for a desktop with no controller, and the first run read `w.kb` as null for that reason). The walk must not go down the MIDDLE: a warm-up ball pushed ahead of you rolls into the net, `clampBallInside` holds it there, and it wedges in the goal mouth and stops you dead at y 410 — which reads exactly like the clamp not opening up. And the DEL/SPACE checks are measured as a CHANGE rather than against a literal, because the checks above them have already altered the name. ⚠️ **AND IT HAS TO BE READABLE**, which is a separate section and neither half of it shows in any geometry check — the keys were in the right places both times. (a) The letters must be UPRIGHT: `uprightAt` cancels the pitch's quarter-turn, so it only works inside `pitchXform`, and `drawLobbyKeys` runs after the restore — called out there it ADDS a turn and in deck view every letter lay on its side. Measured on a multi-letter label (`SPACE`), because rotated it is taller than wide and upright it is not; a single glyph does not discriminate. (b) The letter must be INKED ON its plate: `rgba(col, a)` handed a non-hex colour straight back, and Grass — the DEFAULT palette — sets `line: 'rgba(255,255,255,0.95)'`, so the plate's 8% wash and the letter's 55% were both painted at 0.95 and the board was a near-white slab with **zero** ink pixels on it. Measured as a luminance gap over the plate in RENDERED pixels, never from the palette hex — a hex says nothing about what alpha did to it — with a direct `rgba()` unit check beside it so a red run says which of the two broke. (c) The label fits INSIDE the plate, since a key is wide in world x and deck view turns world x into screen y. (d) **And it is still a KEYBOARD**, which upright letters alone do not give you: Q..P runs left to right on one level line, the block is wider than it is tall, it sits below the pitch on screen, and standing on a key still reads as `lobbySideOf` −1. Built straight into world x/y the rows came out as COLUMNS ten letters tall up the right-hand side in deck view. ⚠️ Plus **KICK as a press** — the only way to type a double letter, since the dwell must latch until the body leaves the key: two taps on Q give "QQ", a second of the button HELD gives one letter, and walking E-R-T-Y with it held types nothing (the latch is on the button). Driven through `pads.p1`, never by setting `p.kick`, which is overwritten before `stepLobbyKeys` sees it. Both (a) and (b) are sabotage-verified; the `rgba` sabotage reports 0 ink pixels at a plate luminance of 244 | ⚠️ Plus the **team-size stepper** (+ and − squares right of the letters): one press adds a body a side, HOLDING repeats (the opposite rule from the letters — measured against one press rather than a constant, so retuning the rate does not break it), it stops at the per-side cap, − takes them away, the size is floored by the humans on a half and + responds AT ONCE against that floor (a stored value allowed to sink below the floor ignores the next four presses), and a 1v1 really is FIELDED as a 3v3. The bots are asserted to be present as bodies rather than only counted, since the lobby's promise is that the preview cannot disagree with what Start does. ⚠️ Bots are now spawned from inside `step()`, so two lobbies on one seed are compared body by body — `spawnLobbyBot` has to stay deterministic. ⚠️ The floor is driven through `lobbySizeBump` and not by walking, because standing on the stepper puts you past the touchline, which the lobby reads as sitting out — so the body pressing − is never one the floor counts, and the first version of that check measured 2 humans and a floor of 1.
| `yourside` | **Which side are *you* on**, and what that does to the record. ⚠️ The defect: everything that reported or recorded a result read `w.score[0]` as "mine", and the warm-up lobby assigns sides by which half you walked into — so a **5–0 win from the top half was recorded as a LOSS**, with RP, Elo, the streak, the goals for/against and the clean sheet inverted, and the screen saying YOU LOSE over a match just won. Season counted a cleared round as failed and Gauntlet took a life for a win. Holds both directions (a defeat from up there is still a defeat — a helper that always answered "you won" passes a one-sided test), the result-screen title read off the rendered DOM, Killer Lobsters's `forceWin`, and that the scoreline stays in **team order** to match the colour-coded scorebug. Plus: guest names reach the pitch and never the save, `stats` stays one flat object of numbers, and renaming adds no keys. ⚠️ Trap: do not set `p.team = 1` by hand — that measures the helper against itself; walk into a half and press Start |
| `continents` | **The first match you ever play** — one continent against another. Holds: one continent per side, the two different, no country twice, names following flags, and the pairing genuinely varying over 20 draws. ⚠️ The load-bearing one is that `CONTINENTS` covers every `FLAGS` country **exactly once** — it caught five unplaced flags on its first run, and without it a new flag drops out of the draw silently for ever. Also: it is a **one shot** (persisted; "every match until you change a setting" would break `botlook`'s stable-across-restarts guarantee), it stops when `magnetball.sel` appears, **your own seat keeps your profile**, and the attract demo keeps its separate one-country-per-side look. ⚠️ Trap: `localStorage.clear()` is not "storage is empty" — boot writes `.ui`, `.feed`, `.login`, `.lastver`; the signal is the absence of `magnetball.sel`. ⚠️ And suites that are NOT about a fresh device must set `magnetball.firstrun` in their init script, or they measure country flags where they expect shirt numbers (`textplates` did) |
| `targetsdrill` | **Break the Targets** — 60s, five fixed spawns, either goal. Three framework assumptions this cuts across, each of which was a real defect: ⚠️ `clampBallInside` **seals the goal mouth in drill mode** (`gh = -1`), correct for gate/zone drills and fatal here — shots bounced off a closed goal line and nothing scored; ⚠️ `updateDrill` **resets the crossing trail part-way down its body**, so a check placed after it hands `segCross` a zero-length segment — measured with the ball in the back of the net and the score reading 0; ⚠️ `drillBest` compares `t < prev` because every other drill is lower-is-better, which records your WORST run as your record here. Also held: the five spots are identical across runs and all inside the pitch, the player is never teleported to the next ball, the clock ending is a result not a failure, and **every other drill still has a sealed mouth** — ⚠️ measured by calling `clampBallInside` directly with the ball already past the line, because firing at it proves nothing (`drillBoundary` puts a wall across the top, so `collideWall` stops the shot and the ball is contained either way; that version passed a sabotage that opened the mouth for every drill). ⚠️ One honest gap recorded in the file: the trail cut inside the targets branch is **not** sabotage-covered — removing it freezes `px/py` at the spawn point rather than double-counting, and no assertion can tell the difference  ⚠️ **Plus a RENDER block, because everything else here drove `step()` and never looked at the canvas** — which is how the drill shipped unplayable: `renderDrill` never drew the goals, so there was nothing to aim at. Two measurement mistakes recorded in it: counting pixels "not the court colour" round a goal mouth passes on the page background (the box straddles the goal line), and a two-render diff against `drillGoalsOpen = false` is not a "nothing drawn" baseline either — the fallback `strokeRect` draws a line straight across the mouth, so the diff is large either way. Goals and net use the diff; the mouth marking uses ink that is neither court nor background. All four sabotages (no goals, no net, no marking, no spots) fail it | ⚠️ And **the player cannot walk off the pitch**: `integrate`'s clamp sat behind `if (!w.drillMode)`, which was safe only while every drill called `drillBoundary()` and got four solid walls. This one calls the match's own `buildGeometry`, whose boundaries are all `ballOnly` and let a player through on purpose, so there was nothing holding the player in — measured at **x = 3401** against a half-width of 220. Driven by holding the stick down for 900 steps, never by placing a body outside and calling the clamp, which would pass on a build where the clamp is never reached.
| `trapspin` | **A trapped ball swings round you, and an opponent can knock it off.** The swing is measured as a swing rather than as movement — ⚠️ "the ball moved round" is true of a snap too, in one frame, so what is counted is the frames on which it is part-way between where it started and where it is going, plus the turn being one-way and at the declared rate, plus it taking the SHORT way round the ±π wrap (measured from 170° to −170°, where the two directions differ). Release must fire **where the ball visibly is**, not along the facing. The steal side: breaking the trap is what makes another player's kick land at all (the carrier re-plants the ball every step), it must stay broken while they hold KICK, it works mid-swing, it does not reach across the pitch, you cannot steal from yourself, and ⚠️ a **team-mate cannot strip you** — measured, because unrestricted it turned every carry into a scrum and inverted the bot ladder. ⚠️ Two traps: `applyHumanInput` overwrites `p.kick`/`p.faceX` on a human seat every step (every body here is `ctrl:'none'`, driven through `handleBallControl` by hand), and holding KICK does **not** trap on the first frame — `TAP_HOLD` is nine frames of "tap or hold?", so a short run measures entirely inside that window and reads a swing rate of zero; runs must also stay under `TRAP.max` (30 frames) or the trap fires by itself mid-measurement |
| `photolib` | **A hundred saved photos, and a delete you have to mean.** Storing, newest-first listing, the cap (oldest goes), and picking one wearing it **without adding a copy** — re-adding on every pick fills a hundred slots with one face. The delete flow: a plain tap in the grid removes nothing, arming shows Cancel, ticking still removes nothing, the confirm **says how many**, Cancel keeps them all, confirming removes exactly the ticked ones, and deleting the photo you are WEARING takes it off your face. ⚠️ The one that would matter: **the upgrade must not eat your replays** — the photo store arrived as a version bump on the database the replay library already owned, and that handler dropped both replay stores unconditionally (written for a v1→v2 migration where the old schema was hours old). ⚠️ And the obvious way to test that does **not work**: IndexedDB is not shared between `file://` pages in this harness — verified with a bare probe, where a write in one page read back as "store missing" in the next — so "save, reload, read it back" measures the harness and passes whether the guard is there or not. A v2 database is built BY HAND instead and handed to the app's own `repLibOpen` to upgrade. ⚠️ Also: the grid paints from a promise, so poll for the tile count rather than waiting a frame; and every tile/button access is guarded, because a build where a plain tap deletes empties the grid under the test and an unguarded `.click()` dies as a stack trace instead of as the sentence saying what broke |
| `matchstats` | The result screen carries the full scoresheet: a row per player, every number equal to the tally it came from, your row marked, both teams tagged, discs actually drawn, cleared between matches and absent in drills. ⚠️ Plus the three things the **acronym cull** has to satisfy at once, which pull against each other on purpose. It shipped as EIGHT acronym columns (`G A SH SV CL KP PST TCH`) with the key that decodes them at the BOTTOM of the screen — on a phone the panels stack, so the legend sat two screens below the headings it explained — and 52 of a 4v4's 64 cells read `0`, so twelve real numbers hid among fifty-two zeros. So: (1) every heading is a WORD (checked on LENGTH — a heading you look up is not a heading) and no key is left behind; (2) NOTHING was lost — every non-zero stat without a column still appears in the prose line as a number AND a word, which is what stops "less info" quietly becoming "lost info"; (3) it FITS, measured as row `scrollWidth` against panel `clientWidth` in a full 4v4 at **360px**, which is the complaint that started it and the half of it nothing else here measured. Also: a player with nothing to report gets NO line (five spelled zeros is longer *and* says less than silence), zeros are classed so they recede, and `touches` is in neither list — it is the one stat every player always has, so it put a prose line on every single row |
| `balllook` | The Skins card is gone (no `ballSkin`/`playerSkin`, nothing "needs art"), and the ball has nine **drawn** looks: all paint, all differ, none spills outside the ball, the pattern turns with spin while Plain doesn't, picking one persists and shows on the pitch — and no request is made for sprite art | Also that the **pattern is visible on the ball under every palette**: `ballSpotInk` is checked to clear `BALL_SPOT_CONTRAST` for all nine, to leave a readable spot untouched (Neon), to have actually changed the unreadable one (Pool), and — in real pixels — that a patterned ball under Pool is measurably darker than a plain one. Verified to fail when the painter takes the raw palette value. ⚠️ Plus the **Sheep as a silhouette**: the ink covers the ball and the fleece is put back in the paper colour, so a horizontal and a vertical scan through the centre both find fleece in the middle and surround at the ends. The surround is measured against the ball's own **rim** (solid, the rim and the surround merge and the disc's edge vanishes, so the ball reads smaller than the circle that collides) and against the pitch (it must stay the brightest thing out there). ⚠️ The `solo` check counts fleece **regions**, because the obvious measurement does not discriminate: sixteen small sheep cover **0.669** of the disc against one big one's 0.622, so the tiled build scored *higher* and the check passed whichever way round it was. One region against seven separates them cleanly. ⚠️ The sheep's anti-tiling guard **builds the bad layout in the test** rather than flipping a flag on the look: every look is one print per hemisphere now, so there is no switch, and a guard that can only test the shipping value is not a guard.
| `ball3d` | **The ball as a rolling sphere** — render only, off by default. Holds: it changes the ball, the face changes with roll, the roll follows the **axis** it rolled about, **it rolls forwards**, a print lands where **sin(latitude)** says, a print at the equator comes back round, the limb compresses, the roll **unwinds on a rebound** while a real change of direction re-latches the axis, the ball **rolls in a drill**, the texture is baked once per (look, ink), and **the world is bit-identical with it on and off**. ⚠️ Six measurement lessons. (1) **"Pixels changed per step of roll, middle versus limb" does not measure compression** — it reads 209 vs 199 on a correct build, because near the limb the surface barely moves but the texture is denser and the two cancel exactly. Compression is **texture density** (edges per pixel). (2) That density must be **averaged over roll phases**: the pattern is *printed* at sixteen fixed places, so whether a longitude band has any ink is down to where the roll stopped — one frame gave 0.0138 at the limb against 0.0623 in the middle, the exact opposite of compression, and really just an empty strip of ball. (3) **A backwards roll changes exactly as many pixels as a forwards one**, so every check here passed with the direction wrong; measure the **sign** of a known mark's movement, with the spot table overridden to a single print. (4) The latitude probe must use a **small** print and compare on **magnitude** — which end of the strip is the top of the ball is a free convention, and a print's box is centred cos(α) short of its own centre. (5) **A snapshot of the ball across renders cannot catch a draw writing to it** — compare the ball's **key set** instead. (6) ⚠️ And the one that found a real defect: **assert it engages at the radius a phone actually draws the ball**. `minPx` shipped at 7 while the ball draws at **6.56px** on a 390×844 viewport, so the feature did nothing for its likeliest users. ⚠️ Also recorded here because a sabotage *passed*: `BALL3D.wrap` has **no** visible effect — the wrap width cancels between the bake and the painter — so "a print comes back round" tests the bake's patch box against the painter's scale factors, not the wrap | ⚠️ Two more, added after the first fix went to the wrong place and the report came straight back. **The FLAT pattern reverses too**: `sel.ball3d` is off by default, so `rot` and the flat look are what nearly everybody sees, and `rot` was driven by `sp` — a magnitude — so the pattern turned the same way whichever direction the ball went. Held from **both ends**: the sign of `rot` per direction, and that a positive `rot` really is clockwise on screen (measured with a one-off probe look carrying a single off-centre dot). ⚠️ That probe must apply `rot` **itself** — every look does its own `c.rotate(rot)`, and a probe that ignored it measured no movement at all and would have called a completely broken build correct. And **the roll axis is a direction on the PITCH**, so the call site adds `cam.rot`: with the pitch turned, a mark has to move along the ball's own on-screen travel. ⚠️ That probe must call `drawBall` **inside `pitchXform`**, the way `render()` does — called bare, `uprightAt`'s cancellation has nothing to cancel and the painter's `+cam.rot` nets it straight back out, which reported a correct build as broken. ⚠️ Then a third round, after the report came back a second time. The root cause of *both* halves of it was **sixteen identical prints on a regular 90° grid**: a periodic lattice makes the roll direction genuinely ambiguous (a filmstrip rolling right was near indistinguishable from one rolling left) and reduces the chosen look to sixteen small copies of itself. Now two prints, one per hemisphere, the second turned and mirrored — held as **"half a turn differs, a full turn matches"**, i.e. the pattern's period is 2π. Plus **the design must look like itself when facing you**, measured against the **flat painter** rather than an expected number, which catches the π/2 horizontal stretch the longitude indexing introduces (72×50 against 50×50 when the asin pre-warp is removed). Plus **the rate is ω = v/R** off the ball's own radius — ⚠️ nothing asserted the rate at all until a sabotage of the old magic constant passed, which is the third wrong claim this file's sabotages have caught. ⚠️ The timing check is **best of three**: a single pass swung between 0.28ms and 1.48ms for the same build on the same machine, enough to fail on its own.
| `sideview` | **The side view** — a showcase camera for replays and the idle demo. Holds: off by default and off is exactly flat (`cam.sq === 1`, turn decided by `pitchHorizontal()` alone); **replays and the demo only**, never a plain match and never a drill; the seats' input rotation is **unmoved** with the camera live (driven through the real `applySeatRotation`, because `pitchHorizontal()` is answered on a layout change and a camera that turned the pitch behind its back would hand a player a stick 90° wrong); **rectangles survive** the projection, measured on the pitch corners — the reason it is an oblique squash and not a perspective, since `drawPitch` hands `L,T,W,H` to `drawGrass`, `vjPaintVideo` and all fourteen `DYN_FIELDS` painters; bodies stand up by about the cylinder height; **the ball stays round while a disc lying on the plane does not** (the contrast is the assertion — "the ball is round" is also true of a build with no squash at all); bodies paint **far to near including the ball**; the ground shadow is visible; the world is bit-identical on and off; and a replay keeps its own framing now that `computeCam` is reachable from the replay path. ⚠️ Four measurement traps. (1) **Do not scan outwards from a body for "where it stops looking like pitch"** — grass has mow stripes, so the first version reported the ground at a cylinder's base as *brighter* than open pitch; every measurement here is a **difference against the same pitch with the body taken away**. (2) **A bounding box is not a body**: `drawOneDisc` puts a name plate seven pixels above the disc, so a round disc measured at aspect **1.70** and every squash ratio taken against it was meaningless — measure the run of changed pixels that stays *contiguous through the centre*. (3) **The probes are anchored on `screenPt` while the drawing goes through `pitchXform`**, two expressions of one projection; sabotaging the squash out of `pitchXform` alone left `screenPt` still claiming it, the probes landed on bare grass and the suite failed on a NaN in the *ball* assertion, pointing nowhere near the break — so "art was found at the point `screenPt` named" is asserted first, and it is a real invariant anyway (every label is placed by `screenPt`). (4) The shadow and the cylinder are measured by **pinning `SIDE.h`/`SIDE.shadow` to zero** and differencing, because the wall covers the whole footprint and a shadow at the body's own radius changes literally nothing |
| `textplates` | Players wear shirt **numbers** by default (you are 1, your bots 2+, the opposition starts again at 1, no clashes within a side); every glyph from the top-down-football avatar list is present, unlocked, unique and paints; two-character plates scale to fit inside the disc rather than spilling; picking one equips, persists and reaches the pitch live |
| `kickoffhead` | The KICK OFF button **is** the Match section's header: collapsed it's just the green bar with a chevron on its right, expanding shows the mode/field/difficulty pickers, pressing the button starts a match without toggling the section, the accordion still holds, and the sticky offset is measured from the header rather than the button nested inside it |
| `hitstop` | Hit stop is its own slider (0 = off, survives Screen shake being turned off, writes and persists) and the freeze only pays out on a **first touch** whose shot the game has walked forward and seen score: wide, backwards, too soft, dying short, wrong attacker and blocked-by-a-body all get nothing (the block is cross-checked against the real sim), a trapped-and-carried shot gets nothing even though it would score, nothing fires at kickoff or after the whistle, and the prediction is deterministic and provably inert — a whole-world diff across 25 predictions, since `collideDiscs` writes to both bodies |
| `netpass` | Players pass through the net, the ball still doesn't: every net wall is `ballOnly` and no wall blocks a player at all, while the collider itself still works (so the geometry didn't just vanish); a player walking sideways inside the goal mouth gets past the post instead of pinning at `gh - r`, enters the mouth but is stopped by the step-out clamp rather than the netting, and never reaches the net's back; checked on all 30 fields for both ball escapes and player escapes |
| `vjmode` | VJ Mode's four load-bearing claims: **off means untouched** (markings alpha is *exactly* 1 and the seam is a byte-identical no-op on a whole frame); **players are untouchable** — a video deck driven to full opacity over a probe pixel proven to be on a disc leaves it unchanged, which is a claim about draw order, and the suite first passed vacuously reading a black pixel that wasn't a player; **the line floor holds** at `VJ.lineFloor` for any deck value including absurd ones; and **zero sim impact** — 600 steps on one seed, VJ off vs both decks blazing, bit-identical. Plus the master clock (tap → 120 BPM, 240 folded into range, no-tempo fires now not never), both crossfader laws (equal-power with no hole; the cut law *holds full to centre then cuts*), a real −40dB kill EQ, a structured-cloneable snapshot with no audio nodes, an unknown command that must not throw, PANIC by call and by hotkey, a preset round trip carrying no media, and a 4-beat auto-loop reading the master clock |
| `updatecheck` | The "Update available" screen: noticing a new build and asking before taking it. ⚠️ It compares `VERSION`, NOT the service worker — a deploy here is a new index.html and sw.js barely ever changes, so `registration.update()` fires `updatefound` for almost none of them and an SW-based check reports "up to date" through every real release. Served from a real HTTP server with the REAL page and the version edited on disk underneath it, because the whole feature is a fetch of itself: a `file://` page cannot do that and stubbing the fetch would test the stub. Holds: it reads its own version (⚠️ the pattern's source text is in the reply BEFORE the real declaration, so a loose regex matches itself and reports a new build every time); the same build raises nothing; a changed one raises the screen NAMING BOTH versions ("an update is available" with no numbers cannot be acted on, and afterwards there is no way to tell whether the reload worked); it never lands over a live match, and appears the moment one ends; Later silences the AUTOMATIC checks but a manual press re-asks (a button that knowingly answers nothing is worse than one that repeats itself); automatic checks are throttled but a manual one always goes; and Update really reloads onto the new build. ⚠️ A DISCOVERY RECORDED IN IT: offline the check does not fail, it answers from the SERVICE WORKER'S CACHE — right rather than a hole, since a reload falls back to the same copy, so the honest property is "never offers what a reload cannot deliver" and the cache has to be put in step with the running build before the network is cut. ⚠️ And the boot check must be gated to `http(s)`: on `file://` the fetch throws "URL scheme file is not supported" into the console, which broke two unrelated suites that assert a clean console |
| `swupdate` | A deploy has to reach **/settings**, not just the game page. Serves a throwaway site from a temp dir, registers the **real** `sw.js`, changes `index.html` on disk, then asks for it the way the settings stub does (`fetch`, not a navigation). Verified to fail on the old predicate: it served the stale build to both the bare fetch and the real `/settings` route. Also checks a navigation still updates and that offline still falls back to cache |
| `goalcam` | The goal push-in: latches onto the scorer, is **step-locked** (two draws must not advance it), ramps smoothly to `GOALCAM.zoom`, genuinely zooms rather than panning, lands the scorer dead-centre, always lets go of both the zoom *and* the pan, stands down while a replay owns the framing, does nothing with effects off, and leaves the world bit-identical over 300 steps. ⚠️ Two traps recorded in it: sampling the peak after the 1.8s goal state has ended measures the ease-*back*, and the camera centres on the **pitch viewport** (`padL + availW/2`), not the canvas — comparing against the canvas centre read exactly the dock width and looked like broken maths. ⚠️ A third: “it zoomed rather than panned” was written `cam.s > base * 2`, which was only ever true because the shipped default happened to be 5× — the moment the default became a **5% push in 0.10s** it failed a camera working perfectly. It is measured against the DIAL now (most of what the dial asked for), and that a bigger dial gives a bigger push is a separate check | Plus the two **dials**: the sliders reach the camera, a 2.5× setting peaks at 2.5×, a 0.40s setting reaches full in 24 steps rather than 69 (the 0.10s default takes 6), **1.0× means off** (no latch at all), and out-of-range values are clamped rather than obeyed. ⚠️ Also: **a replay is at the original size, and so is the match after it.** Standing the camera down for the duration is only half the fix — `loop()` returns immediately during playback, so `advanceGoalCam` never ticks and `goalCam.t` stays frozen; the push reapplied the instant the loop resumed (1.000 for one frame, **1.799** the next). Driven through the REAL auto-replay rather than a hand-set `replay.active`, because the fix lives on the path that starts one — and the rolling buffer is filled first, or the auto-replay never fires and the trace is a goal with no replay in it. The zoom must be seen to HAPPEN before the replay too, or "no zoom during it" is equally true of a build with the goal camera switched off. ⚠️ Plus **the shake and the flash are released too**, and this one is recorded mainly for the measurement lesson: on an ordinary goal there is nothing to release — shake peaks near 5.8 and is at zero within ~430ms, while a replay does not start until `GOALHOLD.replayAt` (3.0s) — so a probe that samples the natural path reads zero **with the release and without it**, and a sabotage sailed through four green assertions. The real case is a kick landing late in the celebration (play continues through it), so the suite injects one on the last frame before playback. Guarded from both ends: the late kick must be confirmed to have landed, and a kick **after** the replay must still shake, because `juiceReset()` is a reset and a build that simply pinned shake at zero would pass everything else |
| `traillook` | The **trail slot** — what the tell behind a player looks like. Every look draws (and `none` draws nothing), no two render the same picture, every one has a picker swatch through the real painter, and the slot survives a reload. ⚠️ Two lines it holds. A look only DRAWS: where a dot is dropped and how fast it fades stay in `advanceTrails`, which runs in the step loop, because the LENGTH of a tell is how far someone just came — the same history is driven by hand under every look and must come out identical, and the dot count is asserted first so "identical" isn't a comparison of two empty lists. And the BALL keeps its streak whatever the slot says: it is the one thing everybody is tracking. ⚠️ That second check first fired the ball across the pitch WIDTH at full speed for 26 steps, which put it through the side wall and into a goal, so the two runs were comparing different matches  ⚠️ **Third tuning**, and the asymmetry is now asserted as a RATIO (`outSecs`/`inSecs`) rather than two magic numbers, so a retune can move both without the check going quiet. Also: the DEFAULT push must be visible (the previous default was 5%, i.e. a setting nobody could see), and the release must **let go of its subject** — `resetKickoff` teleports every body to its formation, so a camera still following the scorer hauls the view across the pitch mid-drift-out, measured at **704px**. ⚠️ And a latent false pass fixed: the juice-off case never reset the camera first, so it was measuring the previous block's push and only passed because 90 steps used to drain `t` at the old 0.5s release  ⚠️ **And a block that drives the REAL rAF loop, touching `computeCam` nowhere** — because every other assertion here calls it by hand, which is precisely how this suite passed a build where the goal camera did not work at all: `applyGoalCam` runs inside `computeCam`, `computeCam` was called only from `resize()`, so the push never animated and a resize mid-celebration baked the zoom into `cam.s` permanently (the reported "stuck zoomed until I hit fullscreen" bug — a phone's URL bar fires `resize` on its own). Testing the maths is not testing the wiring |
| `surfacefeel` | The playing surface, and the replay's field clock. ⚠️ `PITCH` held ABSOLUTE accel/damp and the world took them on Ice and Mud but took the Game Feel sliders on Grass — so Speed and Grip silently did nothing on two of the three surfaces and switching pitch threw away whatever you had tuned. The surface is a multiplier now, through one `surfaceFeel()` that a match, a drill and a live slider change all go through. Checks the DEFAULT sliders still give the old 0.40/0.905, 0.26/0.955, 0.34/0.86 (else it is a rebalance wearing a refactor's clothes), that the sliders reach every surface, that ice > grass > mud for glide at every feel setting, that no setting pushes damping to 1 (a player who never slows down), and that a drill builds movement identically to a match. ⚠️ `glide` scales the per-step LOSS, not the damping factor — scaling 0.905 by 1.05 is above 1. Plus: an animated field must keep moving through a REPLAY, driven through the real `playReplay` — a replay is not the step loop, so `advanceDynField` was never called and every animated theme froze the moment a goal went in |
| `sprint` | **Sprint**: push the stick all the way and you run flat out on a draining stamina ring; empty it and you are on the tired multiplier until it is FULL again. Held: off by default and the world bit-identical with it off over 900 steps (the point of a default-off gameplay switch), a run lasting exactly as long as the DIAL says at full speed the whole way, the tired multiplier equal to its dial, recovery on its own dial with the speed actually coming back, half a push costing nothing, and bots carrying the same ring. ⚠️ **`spent` is latched, and the suite is what proves it matters**: "slow while the ring is not full" read literally slows you on the second frame of the first run, and that sabotage is caught. ⚠️ The ring is measured as a **difference against the same body drawn rested**, per probe angle — the disc already has a guide ring and a rim within a few pixels of that radius, so an absolute ink count reads 65 of 120 angles with no stamina ring drawn at all. It must be absent when rested, an ARC rather than a full circle, shrink as it drains, and start at twelve o'clock. Palette pinned (`neon`), because a suite that samples pixels has to say which one. ⚠️ Driven through `pads.p1`, never `p.inX` — the pad is read every step and overwrites it |
| `swatchcache` | Picker swatches are cached, in the right two buckets, and handed out as copies. ⚠️ Every option tile is a canvas and the FIELD tiles bake their real texture at full resolution for a 64px preview — a 384-square dither written as ImageData, eleven strips of 60px type, a nebula — while `buildSlotPicker` runs on every slot change and used a throwaway state per tile. Measured at 15.6ms for the field row and 17.3ms for the bundle row: opening the Theme card dropped two frames and every tap inside it dropped another. ⚠️ TWO buckets, because a bundle tile paints with TH swapped to its OWN palette and a palette tile reads THEMES directly — neither can go stale, and putting them in the map dropped on a theme change re-paid the whole bundle row at the one moment you are looking at it. ⚠️ And the caller gets a COPY: the Theme card stacks all six slots while the Ball and Sound cards show one AGAIN, so two tiles ask for the same swatch and handing both the same node MOVES it out of the first row. Also holds the other direction — a field tile must render differently over two palettes, or dropping the live bucket is pure waste and the key is wrong |
| `sectionindex` | The SECTION INDEX at the top of `index.html`, kept honest. ⚠️ It lists MARKER STRINGS and never line numbers, because a line number is wrong the moment anybody edits above it and wrong *silently* — a stale map is worse than none. Every quoted marker must resolve to exactly one place in the rest of the file, there must be at least 40 of them, and the index must not have quietly started quoting line numbers (the failure a reviewer waves through). No browser: a text check, milliseconds |
| `dropin` | Joining a match already under way: the touchline, the START, and evening the sides up. ⚠️ Seats were handed out exactly ONCE, in `startMatch`, so a pad woken after the whistle did nothing for the rest of the match — and the first fix over-corrected, taking a bot over the instant any button went down, mid-play, with no side pick and no say in when. Five things, which pull against each other on purpose: (1) a new pad gets a body OUTSIDE the pitch and the roster is byte-for-byte untouched — that is what makes appearing on CONNECTION safe, and it stops being safe the moment it touches the match; (2) it comes on only AT A GOAL and only if it asked — checked by arming it and then driving 60 steps of PLAY, because "they join between goals" is the whole timing rule; (3) the side is the half it stood beside, with undecided falling to what Extra controllers already means; (4) the match GROWS (3v3 → 4v4, 1v1 → 2v2) and shrinks back, with the CAP held against 14 arriving pads and the FLOOR read off the kickoff roster rather than `mode.per` (the lobby fields six a side on a 4v4, so a floor of 4 would strip two bots off each side); (5) whole world hashed over 600 steps with the machinery firing and without. Also: the walk-on goes through the gate and the AI is suppressed for it, unplugging keeps name and stats and leaves the player on the scoresheet via `matchRoster`, a returning pad reclaims its own body, `evenUpSides` is idempotent, and the touchline prompt is drawn and honest. ⚠️ Traps recorded in it: `PADS = []` before `startMatch` or the pad takes a seat at the whistle and there is no drop-in to test; the state gates are polled WITHOUT stepping (`w.state='warmup'` on a match not built as a lobby has no `w.lobby` for `stepWarmup`); and `localStorage.clear()` must stay OUT of `addInitScript`, which runs again on the reload the persistence check depends on |
| `tilt` | The phone-tilt parallax: FOUR depths, and none of it touching the game. The stack is checked as an ORDER (turf behind markings behind bodies behind UI) rather than four magic numbers, so retuning `TILT` cannot quietly break the thing the constants are for, and the turf/markings gap is held SMALL — the touchline is a marking and the grass is the turf beneath it, so a real gap stops reading as a bevel and reads as a misaligned pitch. The HUD's shift is checked on a real BUTTON's `getBoundingClientRect`, not just the wrapper's style string: it moves by a CSS transform precisely so the tap target goes with the picture, and a pause button drawn 9px from where it can be pressed is worse than one that does not move. The RESTING thumbstick marker rides the UI layer and a LIVE one must NOT — a control being touched is attached to your thumb, while one at rest is decoration. Five more things: (1) RENDER ONLY — whole world hashed over 600 steps with the tilt swinging hard and with it flat; (2) the two layers move in OPPOSITE directions and the SHADOW stays on the ground, because a shadow that travels with the body is a sticker and the gap opening between them is the entire height cue; (3) STEP-LOCKED — a draw must not advance it, and one step must move it toward the target without arriving; (4) the neutral position DRIFTS to however you are actually holding the phone, and the first reading is adopted outright (else "level" means flat on a table and playing lying down pins it at full deflection); (5) off on desktop, off under `prefers-reduced-motion` (emulated for real), off when the setting says so. ⚠️ **FOUR measurement traps, all of which produced confident wrong answers here.** (a) `render()` is only idempotent once the SCREEN SHAKE has decayed — shake jitters the whole pitch by `Math.random()` on every draw, so with any left over two shots of the same state differ everywhere and a difference probe reports the entire window as the body: 58px of "growth" for a 7px lift. `decayJuice` is step-locked, so a block that never steps must wind it down by hand, and leftover sparks and a latched goal camera do the same thing for the same reason. (b) Colour-matching the team ink finds nothing — a Classic disc is mostly the player's OWN colour with a face over it and a thin team ring. (c) "The darkest pixel in a row" locks onto the disc's own rim. (d) A window wide enough to include the NAME PLATE measures the plate's adaptive alpha wobbling instead of the parallax. What works is differencing the same frame with and without the body, in a window tight around the disc and its shadow. ⚠️ And: three different values named `lift` across four spread result objects silently clobbered each other, so a passing check printed a wrong number and a failing one blamed the code |
| `seatcontrols` | **The controls, simplified — five things that are one idea.** (1) the keyboard and the FIRST controller drive the same player: a pad in seat one used to set `ctrl='gamepad'` and `pollKeys` was called from a DRAW behind "is there a `human1`", so the keys were not even read — checked as stick-alone, keys-alone, both (no double speed), KICK from either, and that the SECOND pad's seat is NOT shared. (2) SELECT turns a seat a quarter turn: four DISTINCT headings, wraps on the fourth, per-pad, and survives the next match. (3) the bottom-right icon turns with it — that row is the feature's only readout. (4) every button kicks except Start, Select and ⚠️ the **D-PAD**, which is a button as far as the API is concerned, so an unqualified "any button" fires a shot on every step. (5) the lobby prompts are off the pitch. ⚠️ Position is asserted from the row's **recorded y**, not a colour probe: the prompt plates and the players' NAME plates are the same colour and the name plates are legitimately on the pitch, so a pixel count read 1,265 "prompts over the play" on a build where they are plainly below it |
| `fourpads` | **Four controllers, plugged in, playing.** The bug: four pads connected drew four icons, reported "4 controllers detected", brought up the warm-up lobby, and handed out ZERO seats, because `sel.controllers` defaulted to `off` and `padsTakeSeats()` is the only thing that reads it — the same shape as the Steam Deck bug one layer up. ⚠️ "A seat was handed out" is NOT the claim: a seat driven by the wrong pad, or four pads sharing one, looks identical from `w.players`, so every pad is pushed ON ITS OWN and must move ITS OWN body and nobody else's. Also: KICK reaches exactly one seat, START gets four people out of the lobby, one pad drives YOU rather than an opponent, and with no pad connected nothing changes and the keyboard still moves you. ⚠️ Two traps: bots parked at 9e4 are dragged back onto the touchline by `integrate`'s clamp and can land on a seat; and seats 100 units apart COLLIDE — a tidy 65.1/14.7 on all four pads looks like consistent cross-talk and is physics |
| `deckstick` | **A Steam Deck in every shape it actually reports itself as** — standard; non-standard with the two TRIGGERS resting at -1 in front of the stick; non-standard with the D-PAD arriving as an axis pair (a hat) BEFORE the stick; and Steam Input sending the D-pad as arrow keys alongside a gamepad stick. ⚠️ Shape C is the one that found the real bug: with overlapping axis pairs the chooser picked (hatY, stickX) as "the stick", so pushing right and pushing down gave the SAME heading and eight directions measured as six. Each shape is checked for eight DISTINCT headings, that the D-pad never kicks, and that SELECT's quarter-turn composes with the deck layout's own. Shape D additionally proves the arrow keys drive the SAME seat as the stick and that holding both is not double speed. ⚠️ **MEASUREMENT TRAP, and it cost a red run**: the other bodies and the ball were shoved to 9e3 to get them out of the way, which parks nothing — `clampBallInside` and `integrate`'s bounds+20 are hard containment backstops, so both are dragged back to the touchline on the very next step and the 1v1 opponent then chases the ball into the body being measured. It read as the keyboard/stick merge ADDING rather than taking the louder axis (62 alone against 162 together) and it was a collision. Everything else is re-pinned to a far corner **after every step** and the match seed is pinned, so which run it happens on is not a lottery — it passed alone and failed in the parallel pool purely because the seed came off the clock. Same trap `fourpads` records for bots |
| `padstick` | **The move stick on a pad that does not report a standard mapping.** `axes[0]/[1]` is the left stick only under the STANDARD mapping; a non-standard pad numbers its axes however it likes, which is why a Steam Deck moved on the D-pad and not the stick. Holds: a standard pad is untouched; a non-standard one is found anyway; all EIGHT directions give eight DISTINCT headings ("they all move" is true of a build reporting one direction for everything); diagonals are no faster; the D-pad still works; a hand-bound pair wins and clearing it goes back to automatic; and the deck menu reads the stick through the same helper. ⚠️ The fake pad puts two TRIGGERS at rest (-1) **before** the real stick — a trigger never centres, so an untouched one reads as a stick held hard over, and that is the layout that makes naive auto-detection pick wrong. Verified by sabotage: hardcoding axes 0/1 again collapses all eight pushes to ONE heading |
| `padkick` | A is the kick button on every pad, not just the ones reporting a standard layout. ⚠️ The shipped default was the literal index `0`, read as an exact button — `0` is A under the STANDARD mapping, but a pad reporting non-standard numbers its buttons however it likes, so the kick pointed at whatever was numbered 0 and silently never fired. Default is `null` now = A plus the usual fire set; a button bound in Controls is still exact. Driven with fake pads through `padKickHeld` and `gamepadPad`, never by setting `p.kick` (which the next step overwrites). Also: the legacy `0` is folded to `null`, but a DELIBERATE bind of kick to A is kept — without `kickBound` the fold would undo a choice just made — and the Controls readout says what an unbound kick actually does rather than showing a dash |
| `themeslots` | A theme is a collection of six slots and "Custom" is derived: every slot is a real registry whose every option names itself; Pool sets all five (including `sel.snd`, checked against the arrays `playSfx` reads) and reaches the live render; changing one slot names it Custom, unmarks every bundle tile and leaves the other four alone; putting it back brings the name back, and assembling Pool slot-by-slot from Neon gets Pool's name too; a hand-picked sound reads Custom rather than throwing; every set's every variant exists and plays; the ball slot moves in both the Theme and Ball cards and the sound set in both the Theme and Sound cards; every option in every slot renders, plus a starfield over the Grass palette; and a legacy `theme`/`ballLook` save migrates to its bundle once, keeping a hand-picked ball look and a hand-picked whistle |
| `kqberry` | Killer Lobsters berries and the hive: six floaty purple bodies spawn mirrored top and bottom inside the spawn ring, a berry reaching a goal **banks** into that end's hive rather than scoring, filling `BERRY.cells` wins the match outright and the result screen says HIVE rather than SNAIL. ⚠️ Two traps recorded in it. The first build parked a banked berry at (99999, 99999) — the way the code parks anything it wants ignored — and `clampBallInside` dragged it straight back onto the pitch to bank again; being out of play is a FLAG, honoured by `integrate`, the ball-vs-ball pass, `checkGoal` and the draw, and the suite sabotage-checks both by moving a banked berry and by batting it with a player. The second: berries spawn off `w.rng`, which `botInit` seeds — spawned before that call they fell back to `Math.random` and the opening layout differed every match, so two matches on one seed are compared position by position and a 40s run is replayed whole. Plus pixel sampling that a filled cell is drawn purple inside the net pocket and its neighbour isn't, that each goal's hive belongs to the team attacking it, that the float bob is step-locked (three draws must not advance it), and that none of it leaks into a normal 1v1 |
| `determinism` | Checkpoint 1 from the audit, standing as a permanent guard now the audit is CLOSED at same-engine reproducibility: the WHOLE world hashed at frames 600 / 1800 / 3600 across two runs on one seed must be identical, in 4v4 and in Killer Lobsters, while a different seed must give a different match and the world must actually have moved (a suite that compares `score` passes on two completely different matches that finished level). Plus the one rule everything rests on — `Math.random` never reached from inside `step()`, trapped with a **throwing** stub rather than a counter, which is how it caught `spawnKickFx` and the audio noise fill |
| `bigcourt` | Leviathan (2640×4640, 4× Colossus) and the render-only body size floor. The ball never leaves over 90 seconds, goals still happen, it takes 20.3s to run its length, and the goal stays a third of the width like every other court. The floor takes a 2.25px disc to 7px on Leviathan and is **exactly 1** on Classic — not about 1, since a floor that quietly scales every disc on an ordinary court is a redesign. ⚠️ And it is render-only in the only sense that means anything: the same seed stepped 900 frames with the floor honoured and with it forced to 1 must leave the world bit-identical |
| `boxrule` | Goal box occupancy — one keeper, one attacker. Four defenders packed on their own line become one; an attacker may join and does **not** evict the keeper; the slot is sticky over 240 steps (recomputing "who is deepest" every step made two defenders trade it and shove each other out on alternate frames) and is released when its holder walks out; both ends are policed; the enforced edge is the **drawn** edge, probed a hair either side of it; off means off; training and drills are exempt. Plus a bot sanity pass — goals still go in and nobody vibrates on the box edge, **pooled over three seeds**: ⚠️ on one pinned seed the same comparison read 15-vs-6, 18-vs-14, 6-vs-25, 10-vs-11 and 7-vs-51 across five seeds of one build, so two hours of 4v4 is a sample and not a measurement. A margin tight enough to catch a real jam fires on seed luck; one loose enough to survive it catches nothing. It failed exactly that way on an unrelated goal-post change where the pooled figure showed sticking had gone DOWN threefold. ⚠️ Two traps recorded: leaving the AI running measures bots wandering off rather than the rule, and stacking the edge probes on x=0 makes the player being pushed out collide with the one outside, which reads as the rule moving someone it never touched | ⚠️ Plus the **half-second grace**: nothing may touch you for `GOALBOX.grace` after entering, measured as a DIFFERENTIAL against the rule switched OFF — two identical runs whose traces must match EXACTLY inside the window and part after it. A velocity threshold cannot see this: `integrate` damps every player every step, so the first version read vy falling 3.2 → 2.9 and called ordinary damping a shove. Also: the window has to be USED (the runner genuinely gets deeper), grace is a DELAY not a bypass (capped once it engages, and carried out on release), the clock resets when clear, and the slot HOLDER is never on a clock. ⚠️ And no TELEPORT: clamping straight to `GOALBOX.hard` the frame the clock expires yanks the player back, because half a second at pace puts them ~98 units deep on Classic against a backstop of 16 — measured at 98.4 in the suite. ⚠️ `pickIdx` exists because with the rule OFF there is no `w.boxLock` at all, so the off-run cannot find "the non-holder" and must be told which body to follow.
| `icons` | The drawn icon set that replaced the menu's emoji: every registry entry draws a real shape (not a sliver or a typo'd path), no two share a path (two things looking identical is worse than the emoji were), none carries a baked colour instead of `currentColor`, every declared `icon:` exists, and the functional tables are **fully** converted rather than half. ⚠️ The line it holds: `CAPS`/`EYES`/`ANIMALS`/`TEXTS` must have **no** icon field — there the emoji is the item and `paintCap` draws that exact glyph on the disc. ⚠️ And it checks the ⚡ collision explicitly: match-length Quick and difficulty Elite share an emoji and must resolve to different icons, which fails the moment somebody reintroduces a by-emoji lookup. Plus: nav tiles all painted with no leftover emoji, an unmapped entry keeps its emoji, an unknown icon name falls back rather than rendering nothing, and the difficulty ramp is monotonic with all seven steps drawn |
| `discskins` | The guide ring every theme must have, and the rotation choice. Pixel-checks **every** skin in the registry (not a list somebody remembered to update), both teams, all the way round — which caught the ring being invisible on Mono's white disc and drove it to two tones. Plus: rotation on turns the body, off points it up its OWN pitch, a player facing exactly along x doesn't get the fallback (`faceY === 0` is falsy), direction-drawn skins change with the setting while round ones do not, the two-frame leg animation is driven by distance and not a clock (a draw must not advance it, it stops when the player stops, frame 0 is the rest pose, and faster travel means more flips over the same steps), the two sides differ by SILHOUETTE (crab stubby vs lobster long, measured off covered pixels — the discriminator has had to change with the pairing twice, which is the argument for measuring rather than asserting it), all three creatures are still in the registry with more than one pairing offered, the shrimp is still the curled one, a legacy save on the old single-skin key migrates, and neither body breaks out of its ring in either walk frame, and the Shrimp bundle sets field+discs, plays a minute with no escapes, and has a seabed that is scattered once rather than re-rolled per paint. ⚠️ Two traps: the court isn't one flat colour under every theme, so compare the same angle before and after rather than against a background sample; and call the real `strokeDiscGuide` rather than reconstructing it, or the test passes while the shipped painter is broken |
| `hints` | Progressive disclosure of the help text. ⚠️ The line it holds is that **no copy is lost**: every paragraph is still in the DOM in full (3,963 characters across 22 folded + 3 always-open), captured and compared rather than trusting that folding is not deleting. Plus: everything starts collapsed and nothing persists, one toggle per folded paragraph and it opens **its own** without opening the others, aria tracks the state, gameplay-affecting help has no toggle and is never folded, a data readout borrowing `.hint` styling is marked rather than silently skipped (so "unclassified" means a real bug), and two consecutive rebuilds do not add toggles — which caught six paragraphs that `buildSettings` creates itself sitting permanently expanded |
| `warmupoffer` | Warm-up is only **offered** where it has something to do. The lobby exists to test a stick, walk onto a side and calibrate a cocktail seat; on a phone with no controller there is none of that, so `#warmupBtn` and the result screen's Warm-up option were a trip to an empty room. Checks both are hidden there and that **Restart and Main Menu remain** — hiding a button is only correct if the screen still answers "what now?" — that desktop is untouched, and ⚠️ that each of the four things which give warming up a job puts it straight back: a connected pad (**with Input on** — `controllers` defaults to `'off'`, so faking only the hardware proves nothing), "Everyone", cocktail, and *not* "Skip" (that means don't auto-enter; the button is the manual way in). Plus a live re-sync: a pad connecting mid-menu brings it back and unplugging takes it away, without a reload. ⚠️ Verified to fail in **both** directions — a `warmupUseful` that always returns false passes every hiding check while having deleted the feature, so the restoring half is the half that matters |
| `replayfile` | Saving a replay to disk as plain `.json`. Save clip writes a **video** — right for sending someone, wrong for keeping; a replay file is the replay itself, so it re-renders at your screen's size in a few tens of KB. ⚠️ The sharpest check loads a file into a page that has **never played that match** (different field, different mode, fresh world): `drawReplayFrame` reads the field geometry and every player's look off the *live* world, so anything the format forgot to carry fails there and only there. Also holds the magic/version guard (JSON that merely parses is not a replay — ⚠️ **load-bearing now the extension is generic**: the picker offers every JSON on the disk, so this guard is what stands between a `package.json` and a stack trace on a menu), the **row-length guard** (a short row indexes past the end and fails as a BLANK SCREEN rather than an error anybody can read), that every refusal message is actionable, and that playback puts the live world back untouched — the loader swaps the global `world`, which is only safe while `loop()` yields. ⚠️ Two measurement traps recorded here: the render check must count **coverage over a known fill**, not "the canvas differs from before" (`playReplayFile` calls `render()` on the way out, so deleting the playback entirely still passed); and the frame-data check must vary the players and the ball **independently**, because "two frames look different" passed with every player pinned at the origin — the ball alone moved | ⚠️ Plus the **whole-match replay**, which is a different buffer from the goal one: it covers the elapsed time (checked against the STEPS taken, not against a frame count on its own), marks **every** goal with the count cross-checked against the score, survives a body joining mid-match with every row still matching its own player count, and — with the cap lowered so the path is actually taken — **halves its rate rather than stopping**, which is the one thing a truncated recording would still pass every other check for. Plus **playback honours the recorded fps** (30 frames at fps 30 must take twice as long as at fps 60). ⚠️ Two harness traps: stepping through a match starts an auto-replay per goal, and `playReplayFile` returns immediately while one is active — both timing runs came back **0ms** and the comparison was between two numbers that never measured anything, so the probe now clears the replay first and asserts it played something before comparing. And the size of a match file against a goal file is a statement about how long the test match happened to be — compare **time covered** instead. Also holds the button placement: no Save clip on the in-match bar, Save clip plus **two distinct** replay saves on the result screen. ⚠️ Plus four bug fixes with their own traps. **A replay that outlives its world**: `toMenu()` nulls the world, the pending tick reads `world.field` off it, and a throw inside a rAF is SILENT — `finish()` never runs, `replay.active` is stranded true for the page, and every downstream feature quietly does nothing. Measured on a **phone** viewport, because that is the branch of `toMenu` that nulls the world. ⚠️ Two independent guards fix it, so a sabotage of either alone PASSES; verified by removing both, which fails on the flag, on zero ink, and on the real page error. **The lead-in dial** is measured as frames actually held at 2s/6s/15s — a build that fed the setting only to playback would replay the same six seconds and still pass "the setting exists". **Save clip** must return a reason rather than a bare return, and report to the button it is HANDED: it looked up `#clipBtn`, which was deleted when the button moved to the result screen, so every message went to `null`. | ⚠️ Plus the watch-from-menu behaviour: it HOLDS at the end with the transport up, the play button offers another watch, and the bar's exit is what leaves. **The caption check is a DIFFERENCE**, never an absolute pixel count — the halfway line and centre circle are close to the accent colour and sit exactly where the caption would, so "few accent pixels in the middle band" read 103 with the label already gone. The same frame is drawn with `controls` on and off, and the interrupting case is asserted to STILL draw it, or the check passes by deleting the feature. ⚠️ And a chosen replay's promise no longer resolves by itself, so a bare `await` on it is a hang — twenty minutes, once. Plus auto-record (OFF saves nothing first, or ON proves nothing), names leading the row and reaching the filename, and the two panes filled by kind. |
| `showmode` | **Show mode**: the menu cut down for a guest — eleven cards become two, the search index 559 rows becomes 72. Checks the three things that survive (KICK OFF, Warm-up, the Pitch tab) plus How to play, and that the search box, the update check and **Reset all settings** are gone. ⚠️ The check that matters is the **search INDEX**, not the pixels: `display:none` hides a card from the eye and from nothing else, so `menuSearchIndex` is measured directly and spot-checked by name ("party modifiers", "vj mode", "game feel"). Verified to fail when the CSS hides but the index isn't filtered — the exact bug this suite exists for. ⚠️ Pinned from **both** ends: a predicate that always returns false passes every "is it gone" check while having deleted the menu, so the off state and the round trip are checked as hard as the on state. Plus the pause-screen toggle (label states which state it is IN, persists across reload, absent from the result screen) and that the **default is off** — which is what makes `audit`'s reachability claim true, asserted here because `audit` failing would point nowhere near it |
| `replaysection` | The menu's **Replays** card: pick a saved `.json`, watch it, come back. Checks the card is reachable (jump bar + search), that it **states where files go** — folder from `downloadPathHint()` per platform, filename generated from the real `repFilename()` so the example can't drift, and never a fabricated absolute path — that watching **hides the menu** (on a phone `#setup` is a full-bleed fixed screen over the canvas, so the replay would play perfectly and be invisible), that it returns to the card from **both** the end and an early exit, and that a **cancelled** pick still returns (dismissing the dialog fires no `change`, so a promise waiting only on that is a menu that never comes back). Show mode hides the section like any other. ⚠️ **Selector trap recorded here**: `#setup [data-sec="replay"]` matches TWO nodes — the card and its jump-bar chip — and the chip comes first in the document, so `querySelector` returns the chip. Every early reading was of the chip, which made rebuilt chips look like a detached card and returned an empty `getComputedStyle`. Qualify with `.card`. Verified to fail on four sabotages: menu left up, no return, hand-typed filename, cancelled pick |
| `forceupdate` | The four things that shipped together: an update check every launch, a **persisted 30-day deadline** after which the game stops, the player-facing **changelog**, and the replay **library** that makes delete possible. Deadline: a clean install has none; a newer build starts the clock; the **same** build seen again does not restart it (a daily player would never reach the deadline) while a **different** one does; **going offline does not reset it** (`updNote(null)` must not clear the record) and an overdue record **blocks at boot with no server at all**; the gate stops `running`, has no Later, says what is required and for how long, and releases the instant the update lands. Changelog: every entry a real version, the running build at the top and marked, **one generic line per release for bug fixes**, and scanned for developer jargon; the modal fires once per update and shows only the release you landed on, while a **first-ever visit records the version and shows nothing**. Library: rows listed with size, 44px buttons, **delete takes two presses** (as does delete-all), an empty library explains itself, saving writes **both** the file and the entry, and a row's Watch goes through the same hide-menu/play/come-back path as a file. ⚠️ **This suite found a real bug**: the gate and the changelog were wired below `if (!updPossible()) return;`, so on a `file://` page — where every suite runs — neither ever fired. ⚠️ Also holds the two fixes that followed: **versions are compared, not diffed** (`r.v !== VERSION` is true when the running build is NEWER, which bricked the game — offline nothing could clear it — and had a rollback deploy counting down to an older build; the 12-hour clock is checked across noon and midnight, and an unreadable stamp is refused rather than guessed), and the library's **two-store split** (a metadata row must carry no frames, 68ms → 1ms to list; delete clears both stores in one transaction). Also the **About card**: the version and the update check are INSIDE it (not merely present somewhere — a duplicate left at the top would pass a looser check) and gone from where they were, it is reachable from the jump bar and search, and with no server the status **says so** rather than claiming up to date. Verified to fail on eleven sabotages | ⚠️ Plus the **one-tap version copy**: the block is a real press target (hit-tested at its centre after scrolling to it, since About is the last card and its box is otherwise off-screen where `elementFromPoint` returns null), it copies the running version and nothing personal, and it SAYS it copied. ⚠️ The press is a real `click()` on the element, not a call to `copyAbout()` — verified by deleting the onclick binding, which left a direct call perfectly green. |
| `replaywatch` | The goal **tail** and the replay **transport**. Tail: a real goal is played out (not a hand-set state — the tail depends on `checkGoal` firing and on the goal state still integrating), the buffer freezes at the crossing AND again with the tail, the tail is `REP_TAIL` long, `goalAt` lands inside the replay and survives a save/load, and the kept frames contain real **movement** — the ball carries on into the net — rather than padding. ⚠️ The lead-up check is conditional on whether the ring buffer had filled: a goal can come 4.7s after kickoff, so "there are six seconds before it" asserts the fixture, not the code, and fails for the wrong reason. Transport: rendered 44px targets (⚠️ **measured as boxes, never the class** — inside `#hud` the bar was `hidden`-free and 0px tall because playback hides the HUD), four speeds, starts at 1×, toasts move clear of it, a stale banner is cleared, pause freezes the playhead and resume does **not** bank the paused time (⚠️ a 1s pause sampled 60ms later: honest ≈1%, banked 16.6% — the first version paused 320ms and allowed 25%, which the bug passed), speed is measured as **progress per wall second** rather than by reading the variable back, a tap on the pitch does nothing, and the exit button ends it. Plus: the goal replay is unchanged — no bar, any tap skips. Verified to fail on five sabotages |
| `digitalpad` | **The on-screen thumbstick is digital** — eight directions and nothing between them. Swept over the whole circle at five magnitudes: the pad reports only `-1`/`0`/`+1`, exactly **eight** distinct vectors come out, and every one of them moves you at the same speed — ⚠️ that last one is the check that matters, because two axes at full tilt is a vector of length **√2**, so a diagonal is 41% faster than a straight line unless something normalises it. Plus: a half push is full speed, a resting thumb is nothing, and there is no ramp just past the deadzone. ⚠️ Driven through the REAL `onDown`/`onMove`, never by writing to `pads.p1` — the snapping happens in `onMove`, so a probe that set the pad directly would test nothing at all. ⚠️ And the probe has to aim at the **move** zone: right thumb moves on a phone, so bottom-LEFT is the kick zone and a probe aimed there never reaches the move branch while every check below it passes on a stick that was never touched. Holds two things that must NOT change: a **controller stays analogue** (stubbed at `navigator.getGamepads` so it goes through the real `gamepadPad` — this is what would notice the snapping being "simplified" into `applyHumanInput`, which every input method shares), and the **keyboard is unchanged**. The knob is measured **in pixels** to prove it follows the thumb rather than the snapped direction — two thumb positions inside ONE sector, so the movement cannot be the direction changing. ⚠️ No `localStorage.clear()` in the init script: that runs on every navigation including the reload, so it would wipe the very save the persistence check looks for and fail as "the setting did not persist" |
| `touchstart` | A touch-only player could not start a match. ⚠️ Measured before the fix: Start was bound to a gamepad button or Enter and nothing to touch, and in cocktail — which forces the lobby regardless of what is connected — an engaged touch player sat in it for **90 simulated seconds** without leaving, because the idle auto-start resets on movement. The suite checks the new on-screen Start shows, picks a side by walking, fills the rest with bots, starts through the same `lobbyStart()` path with balanced sides, hides during a match and during calibration, and makes cocktail calibrate before it will start. ⚠️ It checks the DEFAULT as carefully as the new path: `lobby:'on'` must still skip the lobby entirely for a touch player, because a defect fix that changes what everyone else sees is a second defect. ⚠️ **MEASUREMENT TRAP, and it let a real bug through:** every press here is `btn().click()`, which dispatches straight at the node and does no hit testing — so a control that is drawn, lit and completely untappable passes all of it. `#hud` is `pointer-events: none` and `#lobbyStartBtn` never opted back in, so on a phone every tap fell through to the canvas. The suite now asks the DOCUMENT what is at the button's own centre, and checks it is on screen and at least 44px  ⚠️ Also holds **who calibrates in cocktail**, from both ends: a TOUCH seat must NOT (there is one touch player in this layout and the thumb zone is on the screen they are looking at), a PAD seat must (a stick held at an angle to the screen is the whole thing calibration discovers), and a touch seat's side must still follow Display → Configure player sides, which is the manual route that makes skipping it safe. Both halves matter — a build where nobody calibrates passes the touch half alone, and one where everybody does passes the pad half alone |
| `photo` | Your own photo as a faceplate: a 1600×900 source is stored **centre-cropped at 128²** and under 40KB (a camera-sized image would blow the localStorage budget), it's worn automatically, actually drawn on the disc and reaches a live match, it **never leaves the device** — the real leaderboard payload is intercepted and checked for `data:`/`base64`, and confirmed to carry only the word `photo` — it persists, Remove falls back rather than leaving a blank plate, Reset look clears it, and a refused camera leaves Upload working with a message that says so |
| `menunav` | Menu navigation: each big card shows one pane at a time with chips and panes matching **one-for-one** (a spare chip shows nothing, a spare pane can never be shown), only one pane visible at a time and every pane genuinely visible when its chip is pressed; both cards measurably shrank (599px and 749px, from 6777 and 3121 flat) and the tallest single pane still beats the old flat card; the 11 nav tiles are grouped under three labels with none lost, duplicated or unwired; and the jump bar is sticky, has a chip per section and no more, labels them, opens one section alone and marks which | ⚠️ Now four groups, Game Feel included, and its block holds two things at once: every one of its fifteen controls is in **exactly one** pane, and the two that act on the whole card (the preset row, the reset button) are **outside** every pane and stay visible on every tab. ⚠️ Five chips do not fit a 430px phone, so the row scrolls sideways and the last chip is **scrolled to and hit-tested at its own centre** — `.click()` dispatches at the node and does no hit testing, which is how nineteen assertions once passed over an untappable button. ⚠️ Two of this suite's own measurements were broken and are fixed: `height()` read the match card's `scrollHeight`, which is **zero** because `#matchCard` is `display: contents`, so `cardsAreShort` was only ever testing Your Player and a zero passed the under-1400px check for free; and the worst-pane loop still contained the exact `g==='player'?'player':'match'` bug the comment above it warns about, so the theme and feel panes were never measured at all.
| `chipreach` | **Every menu tab is reachable by the input the machine actually has.** `#jumpBar` and `.subtabs` are one row that scrolls sideways with the scrollbar hidden — right on a phone, where a thumb swipes it — and on a desktop that left everything past the right-hand edge reachable by nothing: a mouse has no sideways gesture, a wheel scrolls the page rather than a horizontal box, and there was no bar to drag. Measured at 1280×900 before the fix: **eight of the twelve jump chips** off the edge (Theme, Display, Sound, Game Feel, Replays, VJ Mode, About, Online), 4 of 7 theme slots, 4 of 7 player panes, 3 of 6 sound categories, 2 of 5 Game Feel panes — and a sub-pane has no other route in, so those settings were unreachable outright. ⚠️ **MEASUREMENT TRAP, and it is why every existing suite was blind to this**: `menunav` and `taptargets` both press a chip by calling `scrollIntoView()` first, **which scrolls the row for you**, so a chip no human could reach measures as perfectly pressable. Nothing here scrolls a row — only the row itself is scrolled into view, which walks the ancestors and leaves its own `scrollLeft` alone — and `scrollLeft === 0` is asserted at the end of every pass so a probe that quietly scrolls one fails rather than passing for the wrong reason. ⚠️ Also separates **vertical** from **horizontal**: with several cards opened in turn a later row is simply below the fold, and scrolling down is a gesture every device has, so conflating the two failed all 27 chips for the wrong reason on the first run. ⚠️ Pinned from **both** ends — the phone must KEEP the single scrolling row, since these rows are sticky and a second pinned one eats the screen the tabs exist to save; verified on two sabotages, removing the wrap (6 desktop assertions red) and wrapping everywhere (the phone half red) |
| `autoadvance` | Neither screen needs a button press to move on: the lobby counts down 30s and kicks off itself, going back to full on movement, on a stick pushed with **no** displacement (leaning into a wall still counts), and on a controller connecting — and never running at all during calibration; the result screen counts down 30s and starts the next match on the same field with the same teams and a reset score, holds the clock open while any input is held, says so in the hint, and stays out of drills |
| `goalbox` | The goal box mirrors the net exactly onto the pitch: it inks in front of BOTH goal lines with two sides, nothing one net-depth further out or one mouth-width further across (so the mirror is exact, not approximate), it's an outline not a fill, it never draws behind the line into the net, it is measurably fainter than the goal line (130 vs 415 ink) so the two can't be confused, and it holds on all 30 fields |
| `namebook` | **A record per NAME**, the way a cabinet keeps one: played/won/lost/drawn, goals for and against, the goals, assists and saves earned, a streak, and its own Elo. Holds that case and stray spaces are ONE person while the name is stored as first typed; that a made-up name ("You", blank) is never recorded, since filing every anonymous match under one entry makes the book a lie the moment two people share a device; that the rating goes through the SAME `mmrStep` the device record uses, so the two cannot drift; that the cap drops the LEAST RECENTLY PLAYED rather than the worst; and that the table is ranked best first. ⚠️ A real `recordResult` then has to write **one row per human, each judged by their OWN side** — a 3-1 in a 2v2 is a win for one and a loss for the other — with no bots in it, while the device's own `stats` still counts the match, because the two are different questions. Plus: it renders in the Record card, a typed name cannot inject markup (built as nodes), and it travels in the game save |
| `mapvote` | The end-of-match map rating is keyed on **(map, players per side)**, never the map alone: the same map at two sizes keeps two tallies, votes never leak between them, and the size follows the bodies actually fielded rather than the mode (a lobby 6v6 on a 4v4 records `6v6`). One vote per match, the tally reads back, it persists, it's absent in drills, and the career list ranks pairs best-first with an honest empty state |
| `dyntheme` | (incl. **Highlighter**: the court is yellow, both discs are white and are separated by a black BAR run different ways — checked as a shape, since two white discs with no mark are the same disc — and the dither is cached, ramps from the ends to nothing at halfway, never goes solid, and rebuilds when the ink changes. ⚠️ It caught the Bayer threshold bug: the lowest matrix value is zero, so `dens > B/16` filled one cell in sixteen even where the ramp said clear. And **VideoSoccer arrowheads**: a team-coloured ring inked right round the body at radius r, nothing drawn where the old 1.55r nose and 1.05r wings used to reach, an arrowhead that points along the facing with a notched tail. ⚠️ Three measurement traps recorded: the court is BANDED so "differs from one background sample" is true of half the empty court; the ring stroke is centred on r so a 0.97r sample sits on its antialiased inner edge and reads as a gap; and the arrowhead is a few pixels across near the tip where its own outline covers it, so a tip sample reports it missing. It found a real bug — `p.faceY || fallback` made a player facing exactly along x point diagonally.)  Dynamic visual themes: the registry is real, the field advances only on a **sim step** (rendering twice without stepping is byte-identical, so a 144Hz screen can't run it fast), and the starfield does move when stepped; `warp` is genuinely black and white by pixel sampling — 81 points across the pitch all satisfy R=G=B, including the discs, which no palette could guarantee since player colour comes from the profile — with a light field and black lines; `pool` paints baize, a dark pocket, and numbered solids vs stripes distinguished by the stripe's white shoulder | The pool table's **cushions** are checked by pixel too: the goal mouth is a gap in the wood (rails used to run straight through it), the play area is baize right up to the boundary and the timber is outside it (wood inside meant the ball rolled over the cushion instead of bouncing off its face), and on a chamfered field no court colour is stranded in the cut corners.
| `gamesave` | **Five things that shipped together, and the first is a correction.** (1) The wind-up ring round a player is SOLID: it pulsed at `3.5 + f*8` Hz, so a full charge strobed it nearly twelve times a second — reported as the circle flashing while KICK is held, and first misdiagnosed here as the screen shake, so this checks the RING and nothing else (two draws of one frame at a FIXED charge must be identical, at two different charges in case a pulse sat at a turning point, plus that the ring is drawn at all or the whole thing is vacuous). (2) The ring's SIZE dial spans a real range, clamps rubbish, and is **render only** — 900 steps at the smallest and largest setting leave the world bit-identical, so it can never be a hidden gameplay lever. (3) Name plates go to ZERO at the strongest point of either fade rule rather than receding to 5%, while still being a ramp (far > mid > on-top). (4) The save-replay button is **not** on the in-match bar (it comes up between a goal and the kickoff), IS on the playback transport, and the result screen still has both saves. (5) The **game save**: it carries settings, player, record and drills as readable JSON, never carries `magnetball.upd` (another device's forced-update deadline could arrive overdue and lock the game), survives a round trip, ignores unknown keys, refuses four kinds of wrong file with a sentence each, and its two buttons are pressable in About with Import arming before it fires. ⚠️ **Trap recorded**: the result screen's save buttons only exist once a match has been PLAYED, so ending one on frame one lists neither and the check fails for the wrong reason |
| `tennis` | **The Tennis palette** — five supplied colours (#336699 court, #339966 surround, #fefff3 lines, #993300 clay, #c6ed2c ball) on an ORDINARY football pitch. ⚠️ It exists to hold a line that was crossed once: this shipped for one build as a whole tennis SET, with a court's tram lines and service boxes painted over the pitch, racquets against nets for players and a seamed ball, when what was asked for was the colours and the name. So most of the checks are that things are NOT there — the slots all fall to their defaults, there is no `THEME_BUNDLES` row, and the withdrawn field, skin and ball look are gone from their registries rather than merely unlisted (a leftover entry is a tile offering exactly what was rejected). Plus, by pixel: the court and surround sampled on the pitch and past the touchline, the court **solid** at nine points across one half, the pitch still carrying its own halfway line ("solid" must not have been achieved by painting over the football markings), the ball acid yellow with its pattern actually showing ON it, ball containment over 2400 steps, and nothing advancing in a draw. ⚠️ **Two of this suite's own measurements were wrong first time.** The stray-registry sweep matched by PATTERN, so `/tennis|court/` flagged VideoSoccer's `vscourt` and `BALL_LOOKS.tennis` — which had been in the file all along, and which the withdrawn `seam` look was a duplicate of. And the solid-court check read [42,55,75] at one sample and called the court striped; it was a player standing there. The bodies are taken off the list for that one render — parking them is not an option, since `integrate` clamps a body straight back inside the bounds, which is the trap `fourpads` and `deckstick` both record |
| `matchend` | Full time eases play to a standstill before the result screen: the whistle goes and the state turns `over` at once (so nothing more can score) but the screen waits, per-250ms travel decays to under 15% of its peak and the sim is genuinely stopped once the screen is up; the result is a side-by-side scoreline above two team panels reading name → players → awards (the score is in neither panel), every player and ribbon under its own team, the winner marked, numbers equal to the tally; plus Warm-up reachable with the Match section collapsed and with no controller connected, and Reset look restoring the default profile live | Full time also has its **own** whistle — a progressive triple (BEE-BEE-BEEEEP) asserted by intercepting `Aud.tone`/`Aud.noise` on the exported `Aud` object rather than parsing source: exactly three separated blasts, short-short-long with the last 3× the first, gliding in pitch, fitting inside the wind-down, different from the kickoff peep, every variant audible, and every themed sound set naming one.
| `smooth` | Movement reads the same on every screen: with the sim held to the same 120 steps, four different refresh rates give an identical ball streak, disc-dot count and shake/flash decay (with a per-frame-decay probe proving the suite can still see the old bug); mid-step the streak starts at the **drawn** ball rather than overshooting it to the sim position; and interpolation measurably smooths on-screen motion (judder 0.01 vs 2.0 raw, 1 frozen frame vs 168 at 144Hz)  ⚠️ **Bambamzone** adds three traps worth knowing. The silhouette ring must sit at **0.62r**: the KO star's points end at 0.86, so probing at 0.88 reads near-zero for the star *and* the bubble. The **player indicator had to become a rim arc** — a filled backing plate covers every probe angle and both sides measured 32/32, the rule defeated by the borrowed idea. And "the sky has stars" cannot be counted as bright pixels in the void: the nebula bloom alone reads 6,612 of them, so **emptying the star list still passed** — it is measured by diffing a paint against one with the star list empty, which was found by running that sabotage and watching a different assertion fail | ⚠️ Plus the shake's **OFFSET**, which is a different thing from its amplitude and was reported as *"when hitting kick, it looks like it blinks"*: the amplitude decayed per step (already checked here) while the offset was re-rolled inside `render()`, once per draw — a strobe at 144Hz, and two draws of one frame that disagree. Measured as: the offset holds still across two draws while the screen is shaking, it moves once per step, a new offset really does change the picture (or the two-draw check is vacuous), and it settles at exactly zero rather than parking the pitch off-centre. ⚠️ One step has to be taken BEFORE the two-draw check or the offset is still zero and it passes on two unshaken frames. Verified by putting the roll back in `render()`.
| `botstuck` | **A ball at rest on the boards used to freeze the chaser for ever.** A dead end rather than a hiccup: the strike waypoint sits off the pitch, the target clamp drags it onto a point the bot already occupies, `botArrive` writes a zero stick and `align` (0.66) never reaches `strikeEnter` (0.85) — so it can neither move nor commit. Measured before the fix: **23 of 28** resting places round the boundary froze it for 877 of 900 steps with the ball untouched. Sweeps all 28 and requires every one FREED, quickly, with no own goal and no body past bounds+20 (the escape skips the target clamp, so that is where a body leaving the view would show). ⚠️ **The metric is TIME TO FREE THE BALL, never "the bot stood still"** — once the stalemate breaks the match carries on and a bot stands still plenty in ordinary play; the first version of this suite reported 474 still frames on a perfectly good build. ⚠️ Also holds the two things a fix like this gets wrong: it must stay out of ordinary play (under 2% of body-ticks over a minute of 4v4, *and* that minute has to contain goals, or "it never fires" is true of a match where nothing happened), and it must be **deterministic** — a random jitter would make the freeze intermittent rather than fixed, which is the version nobody can test. ⚠️ And a scope boundary: it is **off in Killer Lobsters**, whose own balance guard caught it — that mode is decided by how often a full hive beats the ball, which turns on how much the ball is knocked about, and 5 of 8 matches ended on a hive against a ceiling of 4 (7 of 8 with the kick disabled entirely, so it is the CHANGE the mode cannot take rather than the kick). Four wrong fixes are recorded in the source: driving through the ball with the own-goal guard off (freed all 28, one own goal), a team-aware tangent (drives the ball into the end wall it is already on), and an un-latched flag (a limit cycle, 16 of 28 never freed — worse than before) |
| `botai` | Bot AI, all steps: seeded determinism and no `Math.random`; bots write **only** the fields a human's input writes; the oscillation limit cycle is gone; intercept converges and stays on the pitch; goalie/roles/formation slots are distinct and stable; lane checks, apertures and aim candidates behave; bank kicks land within 4 units mean using the **real** wall restitution and ball radius (21.5 for a naive mirror); bots follow the player's trap setting; the difficulty ladder is monotone on GOAL DIFFERENCE across two team sizes, checked both ways round with auto-replay off |
| `mapmaker` | **Build your own field.** A custom field is just a `FIELDS` entry, so a bug here reaches the picker, `buildGeometry`, all fourteen animated themes, drills, map votes and replay files. Holds: the saved map IS a FIELDS entry; ⚠️ **the preview comes from `buildGeometry`**, checked by measuring the CORNER quadrant pixel-against-pixel — a rounded and a chamfered court remove almost the same AREA, so the first metric scored them 1 sample apart and a coverage count would call a hand-drawn preview correct; `mapClean` clamps on the way IN and a cleaned field still builds; a typed name cannot carry markup; the custom boards/net/posts reach the PHYSICS while a built-in court keeps the shipped values; a `wallB` above 1 still contains the ball; a delete drops `sel.field` to `classic`; an edit saves IN PLACE; desktop-only, hidden not disabled, re-answered on resize |
| `botplans` | **Bot types and team strategies.** ⚠️ Pins the ABSENCE of an `influence` type axis — it drags a formation slot toward the ball, so bending it makes the target move fast and the bots oscillate (0.58 against `botai`'s ceiling of 0.5). ⚠️ The headline check is **the difficulty ladder holds INSIDE every plan**, not "no plan is stronger": that claim was measured and is false (Park the bus ≈ +16 on goal difference against the stock AI, Counter ≈ -9; the lever is `depth`, which is also most of what makes a strategy a strategy). Insane must beat Rookie under every shape, played BOTH WAYS ROUND — a mirror check written first scored plans at ±8 and was measuring which half of the pitch the seeds favoured. Also: a missing multiplier is 1 and never `undefined`; no ability axis; press applies only while defending; the type is re-read when a role changes; Mixed never deals a pair twice or deals `standard` |
| `charge` | **Hold to kick harder.** ⚠️ Drives all SIX call sites that used to carry their own copy of the formula — trap release, one-touch, body check, snail boot and both draws — charged against uncharged, and then again with the switch OFF where the two must come out IDENTICAL. That second half is the one that catches a site still carrying a copy, because the charged-vs-uncharged comparison passes on that build too. Also: two controls not one (zero seconds would mean INSTANT full power); `chargeSecs()` clamps; the wind-up ring is gated on the switch; the wind-up is capped by the DIAL, measured in the sim |
| `fireworks` | **The winning goal, the kickoff toss, and touch-to-start.** ⚠️ Fireworks must NOT run on an ordinary goal — "fireworks appeared" is true of the build this replaced — so the check is the DIFFERENCE between a 1-0 and the goal that wins it. ⚠️ The toss must take nothing out of `w.rng`: forced both ways on one seed, the rest of the world has to be bit-identical. ⚠️ Touch-to-start is driven with KICK never pressed, plus a control that standing away does not start it |
| `taptargets` | **The UX batch.** ⚠️ Reach is PROBED outward from each control's centre, not read off CSS — `.infobtn`'s box is deliberately still 20px and the extra reach is an `::after` pad. ⚠️ **A modal over the page makes every target measure 1px**, because `elementFromPoint` returns whatever is on top; the daily-modal check is first in the file so a failure there explains the rest. Also: the info pad does not swallow its own click; the Sound panes match `SFX_CATS`; KICK OFF is above the fold at 844×390; the menu ships no dead controls and none in the search index; and reduced motion defaults quiet **while the toggle still turns it back on**, which is the half that matters |

## Writing a suite

Set the debug flag **before** load, then drive the hook:

```js
await page.addInitScript(() => { window.__MAGNETDEBUG = true; });
await page.goto('file://' + process.cwd() + '/index.html');
const M = window.__magnet;   // live getters for world, sel, stats, + most functions
```

Two traps that have produced false passes here before — both real, both cost a release:

- **Assert the thing you mean.** A "player blocked at the halfway line" check passed
  because the player was stopping on the *frozen kickoff ball* at y=25, not on the line
  at y=15. Position tests away from the centre spot.
- **Don't encode current behaviour.** A deck suite asserted the menu stays open during a
  match, which was the bug being reported. When a test fails after an intentional change,
  check which side is actually right.

`panel` needs a real origin, so it serves the repo over http via `_serve.mjs` — two
`file://` pages are separate opaque origins and a BroadcastChannel between them
silently never delivers.

Two more traps, both of which produced false results here:

- **Poll, don't sleep.** A backgrounded tab has its `requestAnimationFrame`
  throttled to about 1Hz, so a fixed `wait(400)` after a cross-tab change is a coin
  flip. `until(page, fn)` polls instead.
- **Drive the real call site.** `pollKeys()` runs from `drawControls()`, not
  `step()`, so a loop of bare `step()` calls never reads the keyboard — the first
  version of `cocktailkeys` passed only because the render loop happened to tick
  between two `evaluate` calls.

**A statistical assertion is only reproducible on the engine it was tuned on.** The
determinism bar here is same-engine (`docs/DETERMINISM-AUDIT.md`): a pinned seed reproduces
bit-exactly in one browser build, and cross-engine equality is explicitly not a goal. CI runs
a different Chromium from any dev machine — measured, `kqberry` seed 1 gives hive `[6,5]`
locally and `[6,7]` on CI — so any threshold sitting near the noise passes locally and fails
in CI. That kept `main` red for four consecutive merges while every dev run was green, which
is the worst kind of red: real-looking, unreproducible, and eventually ignored. Sample more
seeds and assert a **proportion** with real margin, and measure the axis that actually
separates a good build from a broken one — for `kqberry` that is how many matches a full hive
decides (2/8 vs 8/8), not how fast one fills, where the two builds overlap.

**Two independent guards mean a single sabotage passes.** When a bug is fixed in two
places — a guard at the call site and a guard in the callee — removing either one on its
own leaves the other doing the job, and the suite goes green. That is not a weak test. Say
so in the comment, and verify by removing *both*, which is the build the bug shipped on.

**Manufacture the condition, or you are measuring the happy path.** A check that samples
whatever the game happens to do can be identical on a correct build and a broken one. The
goal shake is frozen through a replay by a real mechanism, but by the time a replay starts
the shake has decayed to nothing on its own — so "no shake during the replay" was true
either way, and the sabotage passed four assertions. If the condition needs a late kick, a
resize mid-celebration or a body joining mid-replay, put it there deliberately, and assert
that you managed to.

Two traps specific to measuring an AI against itself, both of which gave wrong answers:

- **Play every matchup both ways round.** Driving one side manually (the `aiFrozen`
  seam) hands it about a 17-point edge, so Normal-vs-Normal read 0.33 and made every
  other ladder number look meaningful when it wasn't.
- **Don't assert what the sample can't resolve — and pick a metric that can.** A win
  rate throws away almost all of the signal: a 1-0 and a 6-0 are the same number. At
  twelve duels, normal-vs-hard reads exactly 0.500 on wins and 59-45 on **goals**, and
  the two agree on every rung over a 36-duel offline sweep. The ladder now asserts goal
  difference and reports the win rate. Both are checked against an exactly-even control
  (Normal vs Normal, 22-22, `gd === 0`).
- **A replay stalls a synchronous harness, and it cost a quarter of every duel.** These
  duels are a `for` loop of `M.step(w)` with no `await` in it, so a `playReplay()` promise
  can never resolve inside one: the goal state keeps ticking and `replay.active` stays true
  for the rest of the match. Measured at **910 of 3,600 steps** spent celebrating instead of
  playing. That is also nondeterminism from nowhere — an unrelated change that moved the
  goal hold by a second flipped `ladderIsMonotone` while nothing about the bots had changed.
  `M.sel.autoReplay = false` in any harness that steps by hand.

Two traps specific to measuring colour, both of which produced wrong verdicts here:

- **Composite the background.** The page paints with a gradient and every panel and
  pill is `rgba()`, so `getComputedStyle(el).backgroundColor` is `transparent` almost
  everywhere. Walking up to a white default reported ~90 phantom failures per theme
  while missing the real ones — flatten the stack onto `--bg` instead.
- **Wait out the transition.** `body` eases `color` over .2s, so a sample 60ms after a
  theme switch reads the *previous* theme's ink. That looked exactly like a stale
  inline colour and sent me hunting a bug that wasn't there.

Two traps specific to measuring smoothness, both of which wasted a measurement here:

- **Don't sample `ix()` after `loop()` returns.** `loop()` resets `renderAlpha` to 1 on its
  last line, so `ix()` outside the loop gives the raw sim position by design. Reading it there
  measures the un-interpolated ball and makes a working renderer look broken.
- **Measure the jitter you actually have.** Real rAF deltas on this engine are 16.60–16.80ms
  p05–p99. A synthetic ±3ms jitter makes frame pacing look like the problem when it isn't —
  the deficiency was render state advancing per frame, which no amount of pacing work fixes.

Two traps specific to a setting that is DERIVED rather than stored, both of which
would have let a bug through:

- **Prove both directions of the round trip.** The theme's name is computed by
  matching the live slots against the bundle table, so "changing a slot says Custom"
  is only half of it — a `currentBundle()` that ignored one slot passed that check and
  still lied. `themeslots` also assembles Pool slot-by-slot from a *different* bundle
  and asserts the name comes back.
- **A derived value can hold something with no name.** The sound slot is derived from
  `sel.snd`, which the Sound card edits one category at a time, so it can legitimately
  be a combination no set describes. `SLOTS.sfx.name('custom')` would throw — the label
  has to check `keys().includes()` first, and the suite drives that path deliberately.

A third VJ trap, which produced a silently STALE settings page rather than a failure:

- **`hello` is the whole handshake — do not add a second one.** The game's only full
  state push is gated on `!wasLive`, and every inbound message marks the peer live.
  A `vjhello` posted from the panel's build raced ahead of `hello`, swallowed the
  push, and the panel came up showing localStorage instead of the game's live state.
  Nothing threw and no console error appeared; `panel`'s `snapshotAdopted` is what
  caught it. Anything the panel needs at open must ride the existing `hello`.

Two flakes worth naming, from the same check in `keyfocus`:

- **A keydown is delivered asynchronously.** It pressed ArrowUp and stepped
  immediately, reading a pad that had not seen the key yet. An extra round trip added
  while debugging made it pass every time, which is the signature of a timing flake
  rather than a broken feature. It now waits for `pads.p1` to show the key.
- **Don't inherit state from the sections above you.** Even once the key provably
  reached the pad it still failed about one run in eight, because the world could be
  mid-kickoff — where the half-line gate holds the player still. The check was
  reporting a true fact about the kickoff rule and nothing at all about the arrows.
  It pins `state`, `kickoffRule` and the ball position before measuring.

Two traps from the pool table's cushions, both of which made a wrong picture look right:

- **A path helper that calls `beginPath()` cannot be composed.** The cushion is the
  field outline stroked at double width and clipped to the outside of itself via an
  even-odd clip of `[enclosing rect, field path, goal mouths]`. The helper began its
  own path, which silently wiped the enclosing rect — inverting the clip so the wood
  landed INSIDE the play area, exactly the bug being fixed. It looked plausible on
  screen. Scanning pixels across the boundary is what found it.
- **Sample away from features.** The first probes for "is the boundary baize" and "is
  there wood outside it" read `(halfW, 0)` — which is the halfway line and a side
  pocket — and got white and black. Both probes now sit at 0.4 of the way down,
  clear of the line, the pocket and the goal.

Two traps specific to VJ Mode, both of which produced a false pass here:

- **Sabotage your own sabotage.** Moving the video composite to "after the players"
  by inserting it before `drawDiscs` is still *under* the players — the check
  correctly passed and it looked like the test was weak. Verify the sabotage landed
  and does what you meant before concluding anything about the test.
- **Prove the probe is on the thing.** The draw-order check sampled world (0,0) and
  read `[0,0,0]` — the disc's own rim colour, indistinguishable from missing the
  player entirely. It now searches for a pixel that differs from the bare pitch and
  asserts it found one, so the check cannot pass by sampling nothing.

Also: **`getBoundingClientRect` on a ROTATED element gives axis-aligned bounds.** The
collapsed chevron is `rotate(-90deg)`, so its rect "height" is really its width —
widening the button read as a height change and failed a check about height.
`offsetWidth`/`offsetHeight` give the untransformed layout box; the rect is only right
for position.

Also: `drawBundleSwatch` swaps the module-level `TH` for the duration of the paint,
because the disc skins and the ball painter read the live palette — previewing another
bundle without the swap silently wears the current one. If you add a painter that reads
`TH`, it inherits that, and a mixed slot combination (a starfield over the Grass palette)
is a legitimate setting someone can pick, so fall back through the palette
(`TH.dynMark || TH.line`) rather than a hard-coded colour.

Also: with a gamepad connected a seat's `ctrl` becomes `'gamepad'`, so `pads.p1` no longer
drives it — feed the fake pad instead. And in a sideways pitch `rotQuarter=1`, so stick-up
moves the player along world **+x**; measure displacement, not a single axis.
