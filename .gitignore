!.claude/skills/
# Claude Code session config (but keep shareable skills in the repo)
# Keep the repo clean of the usual local cruft.
# Magnetball is a dependency-free static site — nothing to build.
# scratch / local test output
*.local
*.log
.DS_Store
.claude/*
.idea/
.vscode/
/tmp/
Thumbs.db
node_modules/

# Azure Functions API (api/) — its own node_modules and the local secrets file
api/node_modules/
api/local.settings.json
